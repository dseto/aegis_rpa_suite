# BACKLOG DE EXECUÇÃO - CLAUDE CODE

> 🏁 **DEMANDA FECHADA — 2026-07-12.** Aceitação macro (close-backlog, via Fable, revalidada pelo orquestrador): ✅ suíte (9/9 arquivos, exit 0) · ✅ integração (golden_diff exit 0 + evidência bruta dos 3 runs do regression gate real) · ✅ objetivo do plano (gravacao.json preserva todos os eventos, 63 st_/4 sup_ funcionando, Code Generator não emite sup_ por padrão, zero drift de step_id — os 15 ids em correcoes_acumuladas.json continuam válidos). Nenhum commit feito ainda — proposto na thread principal, aguardando liberação do usuário.

**Plano de origem:** [`plano-sanitizer-alta-fidelidade.md`](./plano-sanitizer-alta-fidelidade.md) (3 rodadas de `plan-critic`/verificação independentes em 2026-07-11 — rodada 1, rodada 2 com modelo diferente da rodada 1, e rodada 3 de verificação linha-a-linha contra o código real; todas as correções incorporadas no plano e sincronizadas neste backlog).

> **🚪 Gate pré-T1 — CUMPRIDO em 2026-07-11:** a rodada 2 (modelo diferente, sem contexto da rodada 1) encontrou 2 achados BLOQUEANTES — rename de `STEP_ID_MISMATCH` que quebraria a autocorreção de ordem em `code_generator.py:603` para todo projeto v1, e um algoritmo de interleave (sort global por `position_anchor`) que reordenaria os `st_` entre si. Ambos corrigidos no plano e neste backlog ([SUBAGENTE 04], [SUBAGENTE 05], [SUBAGENTE 06]). A rodada 3 (verificação linha-a-linha do código real + varredura de consistência plano↔backlog) confirmou os 2 fixes corretos e implementáveis, validou TODAS as citações de linha novas contra o código, e corrigiu 4 problemas novos menores (todos sincronizados entre plano e backlog): definição precisa de `_source_indices`/`position_anchor` para steps com `merged_from`/`source_events` ([SUBAGENTE 02], [SUBAGENTE 04]), filtro de ids fora do plano antes do cálculo de ordem no validador ([SUBAGENTE 05]), atribuição de função errada para `code_generator.py:1148` ([SUBAGENTE 06]), e um exemplo de "campo aditivo" (`position_anchor`) em [SUBAGENTE 09] que contradizia a própria Seção 8 do plano. Gate considerado CUMPRIDO — nenhuma 4ª rodada é exigida antes de despachar. [SUBAGENTE 01] e [SUBAGENTE 02] são somente-leitura/fixture (não tocam código de produção) e sempre puderam rodar independente deste gate.

> **🔒 Working agreement do projeto:** toda mudança em `aegis_*` core só é considerada concluída depois de rodar contra navegador real (mocks provam que o código executa, não que a suposição de seletor/DOM se sustenta — ver `CLAUDE.md`, "Working Agreements"). [SUBAGENTE 10] (T7) é o gate final disso — nenhuma tarefa anterior substitui essa validação.

---

### [SUBAGENTE 01] - Captura dos golden files reais (T0a)
> ✅ CONCLUÍDO — hash `dc32ab3ffff26e5ea093ceae4947fbeb033eb291`, DoD revalidado manualmente pelo orquestrador.
- **🎯 Objetivo:** Rodar o Sanitizer ATUAL (pré-refatoração) contra uma cópia do projeto de referência real e congelar `plano_execucao.json` + `gravacao.json` resultantes como golden fixtures do invariante "byte-idêntico" que [SUBAGENTE 04] (T2) precisa provar.
- **📂 Escopo de Arquivos:**
  - Ler: `aegis_sanitizer/sanitizer.py` (não modificar — só executar), `projects/portal_segura/tests/001_teste/gravacao.json`, `projects/portal_segura/tests/001_teste/dicionario.json`, `projects/portal_segura/tests/001_teste/dataset_inicial.json`, `projects/portal_segura/tests/001_teste/project.json`
  - Criar (não existe hoje): `.specs/golden/real_portal_segura_001/gravacao.json`, `.specs/golden/real_portal_segura_001/dicionario.json`, `.specs/golden/real_portal_segura_001/plano_execucao.json`, `.specs/golden/real_portal_segura_001/META.md`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é congelar um snapshot golden do comportamento ATUAL do Sanitizer, sem alterar nenhum código de produção. Passos: (1) rode `git rev-parse HEAD` e anote o hash. (2) Copie os arquivos `projects/portal_segura/tests/001_teste/{gravacao.json,dicionario.json,dataset_inicial.json,project.json}` para uma pasta temporária isolada (ex.: pasta descartável fora do repo ou `.specs/golden/_tmp_run/`). (3) Rode `python aegis_sanitizer/sanitizer.py --project-dir <pasta_temporaria>` (comando documentado em CLAUDE.md, Fase 2) com a variável de ambiente `AEGIS_COGNITIVE_ENABLED=false` — obrigatório: existe `.env` na raiz do framework com chave de API e o `CognitiveGateway` auto-ativa quando há chave e o flag não está setado (`aegis_runner/cognitive_fallback.py:97`); com o gateway ativo, `refine_semantics_with_llm` reescreve TODAS as `business_description` a cada execução, tornando o golden não-determinístico. Com o flag desligado, as descrições já presentes no `gravacao.json` copiado são preservadas e a saída é 100% determinística. (4) Copie o `gravacao.json`, `dicionario.json` e `plano_execucao.json` RESULTANTES (pós-sanitização) da pasta temporária para `.specs/golden/real_portal_segura_001/`. (5) Escreva `.specs/golden/real_portal_segura_001/META.md` com: o hash do commit do passo 1, a data, o comando exato executado (incluindo `AEGIS_COGNITIVE_ENABLED=false` — quem regenerar para comparar precisa repetir o mesmo ambiente), e uma frase confirmando que estes são os artefatos gerados pelo Sanitizer v1 (pré-refatoração) contra o projeto `portal_segura/tests/001_teste`. (6) Apague a pasta temporária. NÃO modifique `aegis_sanitizer/sanitizer.py` nem qualquer arquivo dentro de `projects/portal_segura/`. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `.specs/golden/real_portal_segura_001/plano_execucao.json` existe e tem `"version": "1.0"` (confirma que foi gerado pelo Sanitizer atual, não por uma versão já refatorada).
  - [x] `.specs/golden/real_portal_segura_001/META.md` contém um hash de commit válido (`git cat-file -e <hash>` não retorna erro).
  - [x] `projects/portal_segura/tests/001_teste/` permanece byte-idêntico ao estado antes da tarefa (`git status` não mostra mudanças nesse diretório).

---

### [SUBAGENTE 02] - Fixture sintética da ambiguidade R1×merge (T0b)
> ✅ CONCLUÍDO — sequência final `[st_001, sup_001]` confirmada; DoD revalidado manualmente pelo orquestrador.
- **🎯 Objetivo:** Construir uma gravação sintética mínima que force o padrão "clique R1-duplicado adjacente a um clique merge-duplicado no mesmo widget" e computar manualmente o `plano_execucao.json` esperado, usando o algoritmo de composição JÁ RESOLVIDO na Seção 8 (T2) do plano de origem — isso vira o teste de regressão que prova a implementação de [SUBAGENTE 04] contra esse caso-limite.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (Seção 3 = schema v2 e catálogo de `step_role`; Seção 8, bloco "Composição R1 × merge — RESOLVIDA"), `aegis_sanitizer/sanitizer.py` (linhas 152-201 = regras R1-R4; linhas 831-923 = `_dedup_consecutive_clicks`/`same_widget()`/`choose()`; linhas 925-1102 = `_write_execution_plan`, necessário para calcular à mão os campos derivados de cada step — description/coords/text)
  - Criar: `.specs/golden/synthetic_r1_merge_case/gravacao.json`, `.specs/golden/synthetic_r1_merge_case/dicionario.json` (mínimo, só o necessário pra sanitizer não quebrar), `.specs/golden/synthetic_r1_merge_case/plano_execucao_esperado.json`, `.specs/golden/synthetic_r1_merge_case/README.md`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é criar uma fixture de teste sintética, não código de produção. Construa um `gravacao.json` mínimo e válido (mesmo formato dos eventos reais do recorder — veja exemplos em `projects/portal_segura/tests/001_teste/gravacao.json` para o schema de evento) contendo, nesta ordem, EXATAMENTE: (1) um clique em `#btn-abrir-modal` (widget A); (2) um clique DUPLICADO consecutivo no MESMO seletor `#btn-abrir-modal` (isso deve disparar a regra R1 — cliques consecutivos idênticos, ver `sanitizer.py:161-165`); (3) um clique em `span.mat-button-wrapper` (mesmo widget físico do passo (1), seletor DIFERENTE — exatamente o exemplo de merge da Seção 3 do plano) cujo `parent.selector`/`parent.has_text` seja IDÊNTICO ao do clique do passo (1) OU cujas coordenadas (`x_percent`/`y_percent`, frações 0-1) estejam a menos de 0.02 de distância das do passo (1), e cujo campo `text` seja IGUAL ao do passo (1) ou ausente (o guard `texts_differ` de `same_widget()` bloqueia o merge se ambos os eventos tiverem `text` e eles diferirem) — isso deve disparar `same_widget()` do merge (`sanitizer.py:861-901`). ATENÇÃO — motivo de o 3º seletor ser `span.mat-button-wrapper` e NÃO um backdrop/overlay: o seletor do 3º evento NÃO pode conter `backdrop`, `cdk-overlay-container`, `Nenhum resultado` nem `mat-autocomplete-panel-`, senão as regras R2/R3 (`sanitizer.py:167-188`) o classificam como ruído `keep: False` ANTES de o merge sequer rodar, e o caso-limite R1×merge que esta fixture existe para provar nunca acontece (o resultado degeneraria para 2 `sup_` + 1 `st_`). Resumo do desenho: R1 e merge miram o MESMO widget físico, mas por sinais diferentes — R1 via adjacência de seletor idêntico (evento 2), merge via proximidade de coordenadas/parent com seletor distinto (evento 3). Depois, aplique manualmente — NO PAPEL, sem rodar código ainda, pois `_classify_raw_events`/`_merge_consecutive_clicks` ainda não existem — o algoritmo resolvido na Seção 8 do plano (`kept_events` exclui o evento R1-tagged ANTES do merge rodar; merge nunca vê esse evento) para determinar qual seletor sobrevive como `st_001` e escreva o `plano_execucao.json` esperado (formato v2, com o evento R1-tagged aparecendo como `sup_001` com `step_role: 'raw_duplicate_click'` — nome do catálogo da Seção 3 do plano). Ao calcular o sobrevivente do merge, aplique `choose()` (`sanitizer.py:909-914`) exatamente como escrito no código. No `plano_execucao_esperado.json`, os campos NORMATIVOS (que [SUBAGENTE 04] vai comparar mecanicamente no teste) são: sequência de `step_id`, `execution_hint`, `step_role`, `type`, `selector`, `merged_from` (com os `original_index` dos absorvidos), `original_index` dos `sup_` e `fidelity_summary`; preencha os demais campos (description/coords/text/scenario) segundo a derivação real de `_write_execution_plan` e a Seção 3 do plano, e documente no README.md que `generated_at`/`test_dir` são não-determinísticos e ficam fora da comparação. Documente o raciocínio passo a passo em `README.md`. NÃO crie nem modifique `_classify_raw_events` ou `_merge_consecutive_clicks` — esta tarefa só produz fixtures e o resultado esperado calculado à mão, para [SUBAGENTE 04] usar como teste de regressão depois de implementar o código de verdade."
- **🧪 Critério de Validação (DoD):**
  - [x] `gravacao.json` sintético tem exatamente os 3 eventos descritos, na ordem descrita, e é JSON válido (`python -c "import json; json.load(open('.specs/golden/synthetic_r1_merge_case/gravacao.json', encoding='utf-8'))"`).
  - [x] `README.md` explica, citando o algoritmo resolvido do plano, por que o resultado esperado tem exatamente 1 `sup_` e 1 `st_` (não 2 `sup_`, não 2 `st_`), E por que `sup_001` aparece DEPOIS de `st_001` na sequência final do array `steps` (não antes) — use a definição de `_source_indices`/`position_anchor` da Seção 8 (achado da 3ª revisão independente, 2026-07-11): `choose()` elege o evento 3 (`span.mat-button-wrapper`) como conteúdo de `st_001`, então o `original_index` do evento 1 (o mais cedo do merge) sobrevive só em `merged_from`; `position_anchor(st_001) = min(original_index do evento 3, original_index do evento 1 via merged_from)` = o do evento 1, que é MENOR que o `original_index` do evento 2 (o R1, agora `sup_001`) — logo `sup_001` não é menor que o anchor e cai depois de `st_001`, não antes. Se o README justificar a ordem usando só o `original_index` de topo do step sobrevivente (ignorando `merged_from`), a ordem sai invertida e o valor fica errado — não só impreciso.
  - [x] `plano_execucao_esperado.json` é JSON válido e usa o schema v2 (Seção 3 do plano: `execution_hint`, `step_role`, `original_index`).

---

### [SUBAGENTE 03] - `_classify_raw_events` em sanitizer.py (T1)
> ✅ CONCLUÍDO — 25 testes novos + suíte existente (12+5+3) verde; DoD revalidado manualmente pelo orquestrador (exit code 0 confirmado).
- **🎯 Objetivo:** Extrair o bloco de deleção R1-R4 de `sanitize()` para um método `_classify_raw_events` que TAGGEIA em vez de remover — nenhum evento desaparece de `gravacao.json`.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (Seções 1.2, 2/D1), `aegis_sanitizer/test_sanitizer_execution_plan.py` (convenção de teste do módulo)
  - Modificar: `aegis_sanitizer/sanitizer.py` (função `sanitize()`, bloco atual nas linhas 152-201; método novo `_classify_raw_events`)
  - Criar: `aegis_sanitizer/test_classify_raw_events.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é refatorar SOMENTE o bloco de deleção de eventos dentro de `SanitizerService.sanitize()` em `aegis_sanitizer/sanitizer.py` (hoje nas linhas 152-201, as 4 regras R1-R4 que usam `continue` para pular eventos). Extraia essa lógica para um novo método `_classify_raw_events(self, events: list) -> list` que: (1) para cada evento que hoje seria descartado por R1 (cliques consecutivos idênticos), R2 (overlay genérico/backdrop/'Nenhum resultado'), R3 (painel autocomplete órfão) ou R4 (fill duplicado mesmo cenário+seletor+valor), MANTÉM o evento na lista e adiciona `ev['sanitizer_class'] = {'role': <um de 'raw_duplicate_click' (R1), 'overlay_noise' (R2), 'stale_panel_click' (R3), 'redundant_refill' (R4) — use EXATAMENTE esses nomes: são o catálogo da Seção 3 do plano e viram o `step_role` dos steps `sup_` na tarefa seguinte>, 'keep': False, 'reason': <frase curta explicando por quê>}`; (2) retorna a lista COMPLETA, mesmo comprimento da entrada, nunca remove nada; (3) NÃO estampa `original_index` — a estampagem `ev['original_index'] = i` (ordem original da gravação) é feita em `sanitize()`, num loop simples inserido imediatamente ANTES do Padrão P (linha 135), porque o Padrão P troca a ordem física dos eventos e o `original_index` precisa refletir a ordem GRAVADA, não a pós-inversão (decisão D2 do plano); `_classify_raw_events`, chamada na posição atual do bloco R1-R4 (DEPOIS do Padrão P, mesmas semânticas de adjacência de hoje), apenas preserva o campo já estampado. As condições exatas de cada regra (R1-R4) devem ser preservadas byte a byte — é só o EFEITO que muda de `continue`/descarte para tag. `sanitize()` passa a chamar `_classify_raw_events` e usa o resultado (todos os eventos) como o `raw_data['events']` salvo em `gravacao.json` — ou seja, `gravacao.json` deixa de perder eventos na sanitização. CRÍTICO para não mudar nenhum outro comportamento nesta tarefa (exigência explícita do T1 no plano: 'o relatório e o restante do fluxo interno passam a filtrar por keep != false onde hoje recebiam a lista já podada'): todo ponto de `sanitize()` rio abaixo que hoje recebe a lista já podada — `refine_semantics_with_llm`, a chamada `self._write_execution_plan(events, dataset_rows)` (linha ~257) e o loop de geração do `relatorio.md` — passa a receber a view FILTRADA `[e for e in eventos if e.get('sanitizer_class', {}).get('keep', True)]`, com um comentário `# TEMPORARIO (T1): filtro no call-site; T2 move a filtragem para dentro de _write_execution_plan`. Resultado: só o `gravacao.json` salvo muda (vira superset taggeado); `plano_execucao.json` e `relatorio.md` saem byte-idênticos aos de hoje. NÃO toque no CORPO de `_write_execution_plan` nem de nenhuma outra função — isso é escopo de outra tarefa. NÃO mude a lógica do Padrão P (inversão de autocomplete, linhas 135-150) — apenas insira a estampagem de `original_index` imediatamente antes dele, como descrito acima. Escreva `aegis_sanitizer/test_classify_raw_events.py` seguindo a convenção de `aegis_sanitizer/test_sanitizer_execution_plan.py` (mesmo padrão de `sys.path.insert` + `from sanitizer import SanitizerService`), com casos cobrindo as 4 regras R1-R4 virando tags (não mais descarte), confirmando que nenhum evento some e que `original_index` reflete a ordem ORIGINAL da gravação (em particular: um par invertido pelo Padrão P mantém os `original_index` pré-inversão — a lista fica fisicamente reordenada, mas o rastro aponta para a ordem gravada). Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_classify_raw_events.py` passa.
  - [x] `python aegis_sanitizer/test_sanitizer_execution_plan.py` — rodar e conferir: só é aceitável falhar se a falha for estritamente por `gravacao.json`/comportamento de eventos brutos (nenhum teste desse arquivo hoje deveria testar isso diretamente; se algo quebrar, investigar antes de seguir — não é esperado nenhuma quebra nesta tarefa, já que `_write_execution_plan` não muda aqui).
  - [x] `python aegis_sanitizer/test_weak_selector_enforcement.py` e `python aegis_sanitizer/test_dryrun_multirow.py` continuam passando.

---

### [SUBAGENTE 04] - Reescrita de `_write_execution_plan` para o schema v2 (T2)
> ✅ CONCLUÍDO — golden_diff.py OK contra golden real (63 emitíveis, 4 suprimidos), 16 testes novos + 12 existentes verdes; DoD revalidado manualmente pelo orquestrador.
- **🎯 Objetivo:** Reescrever `_write_execution_plan` para produzir `plano_execucao.json` v2: todos os eventos classificados por [SUBAGENTE 03] entram no array `steps`, com dois espaços de id (`st_NNN` emitível / `sup_NNN` suprimido) e o algoritmo de composição R1×merge já resolvido no plano de origem.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (Seções 2, 3, 4 e o bloco "Composição R1 × merge — RESOLVIDA" na Seção 8/T2), `.specs/golden/real_portal_segura_001/plano_execucao.json` (golden de [SUBAGENTE 01]), `.specs/golden/synthetic_r1_merge_case/*` (fixture de [SUBAGENTE 02])
  - Modificar: `aegis_sanitizer/sanitizer.py` (método `_write_execution_plan`, hoje linhas 925-1102; e os métodos que ele chama: `_dedup_consecutive_clicks` → renomear `_merge_consecutive_clicks`, `_reorder_dropdown_pairs`, `_drop_redundant_select_corrections` → renomear `_mark_superseded_selects`, `_drop_redundant_pretrigger_clicks` → renomear `_mark_phantom_pretrigger_clicks`)
  - Criar: `aegis_sanitizer/test_write_execution_plan_v2.py`, `aegis_sanitizer/golden_diff.py` (script standalone de comparação golden vs. output atual — ver DoD)
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é reescrever `_write_execution_plan` em `aegis_sanitizer/sanitizer.py` seguindo EXATAMENTE o algoritmo de composição descrito na Seção 8 (bloco 'Composição R1 × merge — RESOLVIDA') do plano em `.specs/plano-sanitizer-alta-fidelidade.md` — leia essa seção INTEIRA antes de escrever qualquer linha. Resumo do contrato: `kept_events` (eventos SEM `sanitizer_class.keep == False`) alimenta a construção de `steps` e toda a cadeia de merge/reorder/supersede/phantom EXATAMENTE como hoje (só renomeando as 3 funções conforme a Seção 4 do plano); os eventos EXCLUÍDOS de `kept_events` (tag `keep: False` de [SUBAGENTE 03]) viram steps `sup_NNN` independentes, com `execution_hint: 'skip'`, `step_role` herdado de `sanitizer_class.role`, e `suppression_reason` herdado de `sanitizer_class.reason`. A NUMERAÇÃO `st_NNN` é puramente sequencial sobre a saída da cadeia de merge, IDÊNTICA à numeração de hoje — e a ORDEM RELATIVA de `steps` entre si, uma vez definida pela cadeia, NUNCA é alterada depois disso (achado da 2ª revisão independente, 2026-07-11: um sort global por `position_anchor` reordenaria os `st_` entre si, porque `_reorder_dropdown_pairs` já quebra a monotonicidade de `original_index` hoje — `sanitizer.py:760-761` move deliberadamente steps `between` para depois do select colapsado). Para intercalar os `sup_`, use MERGE-INSERT, NÃO sort: calcule `position_anchor` (variável LOCAL, NÃO persistida no step final) = menor valor dentre TODOS os índices-fonte de cada `st_step` — **definição precisa obrigatória (achado da 3ª revisão independente, 2026-07-11): um `st_step` guarda seus eventos-fonte em campos DIFERENTES dependendo de como foi produzido (step simples: só `original_index` próprio; sobrevivente de `_merge_consecutive_clicks`: `original_index` próprio + lista `merged_from` com um `original_index` por evento absorvido; `select` composto de `_reorder_dropdown_pairs`: só `source_events`, uma lista, sem `original_index` no nível raiz) — `position_anchor` é o MÍNIMO entre `[step.get('original_index')]` (se existir) `+ [m['original_index'] for m in step.get('merged_from', [])] + step.get('source_events', [])`, NUNCA um único desses campos lido isoladamente. Isso não é só rigor formal: `choose()` (`sanitizer.py:911-914`) tipicamente elege o clique MAIS RECENTE como sobrevivente ("Mantém apenas o último clique"), então o `original_index` que sobra no campo raiz de um step fundido costuma ser o do evento MAIS TARDIO do grupo — o(s) evento(s) mais cedo(s) ficam só em `merged_from`. Ler só `step.get('original_index')` (ignorando `merged_from`) dá um anchor ATRASADO para qualquer step que absorveu um clique anterior, incluindo a própria fixture de [SUBAGENTE 02] (evento 1 funde com evento 3; `choose()` tende a eleger o evento 3 como conteúdo; o `original_index` do evento 1 sobrevive só em `merged_from`) — errar esse cálculo pode colocar o `sup_001` daquela fixture do lado errado do `st_001` sem que nenhum teste “sinta cheiro” do bug se o teste também assumir a leitura errada.** Ordene a lista de `sup_` pelo próprio `original_index`; itere `steps` NA SUA PRÓPRIA ORDEM (nunca reordenada) e, antes de cada `st_step`, insira todo `sup_` pendente cujo `original_index` seja menor que o `position_anchor` do `st_step` atual; ao final, anexe os `sup_` remanescentes. O pseudocódigo completo está na Seção 8 do plano, bloco 'Composição R1 × merge — RESOLVIDA', passo 5 — siga-o literalmente, não invente uma variação. Implemente também, dentro deste método: Padrão Q com `has_text_original` + `sanitization_notes` quando houver remoção de token dinâmico (preservando a sanitização atual do valor operacional); achatamento `label:has-text(...) input` → `label` ganhando `selector_original`; serialização de `scenario` (novo — leia do evento, hoje só existe no escopo de `sanitize()`) e `text` (já computado hoje, só não estava sendo serializado); `total_steps` = contagem de emitíveis; novo campo `total_recorded_steps` = contagem total de eventos classificados; novo campo `fidelity_summary` (`raw_events`, `steps_required`, `steps_optional`, `steps_suppressed`, `merges` — ver exemplo na Seção 3 do plano); `version: '2.0'`; herança de `flaky` por `(type, selector)` restrita a steps emitíveis (a lógica de herança já existe nas linhas 936-947 — só restrinja o universo de candidatos). Escreva `aegis_sanitizer/golden_diff.py`: um script standalone (`python aegis_sanitizer/golden_diff.py <golden_dir> <output_plano.json>`) que carrega um golden `plano_execucao.json` (v1) e um plano v2 recém-gerado, filtra o v2 só pelos steps com `step_id` começando em `st_`, e falha (exit code 1, imprime diff) se a sequência ORDENADA de `(step_id, type, selector, description)` e demais campos operacionais — comparada posição a posição na lista, NÃO como conjunto — não bater EXATAMENTE com o golden v1. A ORDEM faz parte do invariante, não só o conteúdo dos campos (achado da 2ª revisão independente, 2026-07-11) — o script deve comparar as duas listas elemento a elemento na ordem em que aparecem, não ordenar/agrupar por `step_id` antes de comparar. Escreva `aegis_sanitizer/test_write_execution_plan_v2.py` cobrindo: (a) o caso sintético de [SUBAGENTE 02] — rode o sanitizer contra a fixture e compare com `plano_execucao_esperado.json`; (b) supressão R2/R3/R4 vira `sup_` com `execution_hint: 'skip'`; (c) `fidelity_summary` bate com as contagens reais; (d) uma lista de eventos em que NENHUM tem o campo `sanitizer_class` (equivalente a uma gravação inteira sem nenhum evento capturado por R1-R4 — não passou por nenhuma tag de `_classify_raw_events`) ainda gera um plano v2 válido, com todos os steps como `st_NNN`/`execution_hint` ausente e nenhum `sup_` (o filtro `keep != false` deve tratar a ausência do campo como `keep=True` por default). NÃO toque em `step_validator.py`, `code_generator.py`, `runner.py` — fora de escopo desta tarefa. Não faça refatoração, renomeação nem melhoria fora deste objetivo (as 3 renomeações de método listadas acima são as ÚNICAS renomeações autorizadas)."
- **🧪 Critério de Validação (DoD):**
  - [x] Copie `projects/portal_segura/tests/001_teste/{gravacao.json,dicionario.json,dataset_inicial.json,project.json}` para uma pasta temporária isolada (NUNCA rode o sanitizer reescrito contra o projeto original — mesma regra de isolamento de [SUBAGENTE 01]), rode `python aegis_sanitizer/sanitizer.py --project-dir <pasta_temporaria>` com o `sanitizer.py` já reescrito e com a variável de ambiente `AEGIS_COGNITIVE_ENABLED` setada como `false` para essa chamada (sintaxe depende do shell: PowerShell `$env:AEGIS_COGNITIVE_ENABLED='false'; python ...`; bash `AEGIS_COGNITIVE_ENABLED=false python ...`), e então `python aegis_sanitizer/golden_diff.py .specs/golden/real_portal_segura_001 <pasta_temporaria>/plano_execucao.json` sai com código 0 (invariante byte-idêntico provado contra o golden real). Apague a pasta temporária ao final.
  - [x] `python aegis_sanitizer/test_write_execution_plan_v2.py` passa, incluindo o caso sintético de [SUBAGENTE 02].
  - [x] `python aegis_sanitizer/test_sanitizer_execution_plan.py` — rodar e listar quais testes passam/falham; **esperado:** só `test_dropdown_pair_with_fallbacks_collapses_without_fallback_field` pode divergir (campos aditivos) — isso será corrigido em [SUBAGENTE 09] (T6), não aqui. Qualquer OUTRA falha é regressão real a investigar antes de fechar esta tarefa. (Resultado real: os 12 passaram, inclusive esse — nenhuma divergência precisou ser tolerada.)

---

### [SUBAGENTE 05] - `step_validator.py` hint-aware (T3)
> ✅ CONCLUÍDO — 17 testes novos + suíte existente verde; DoD revalidado manualmente pelo orquestrador.
- **🎯 Objetivo:** Tornar `validate_bot_against_plan` e `validate_resilience_patterns` cientes de `execution_hint`, validando subsequência obrigatória para `required` e tolerância de ordem para `optional`/`skip` emitidos — sem quebrar o comportamento atual para planos v1 (sem hints).
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (Seção 5, incl. os blocos de correção do plan-critic em 5.2 e no T3), `aegis_sanitizer/test_weak_selector_enforcement.py` (convenção de teste do módulo)
  - Modificar: `aegis_sanitizer/step_validator.py` (funções `validate_bot_against_plan`, linha 349; `validate_resilience_patterns`, linha 732)
  - Criar: `aegis_sanitizer/test_step_validator_hints.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é tornar `aegis_sanitizer/step_validator.py` ciente do campo `execution_hint` (`'required'` ausente/explícito, `'optional'`, `'skip'`) nos steps do plano, seguindo a Seção 5 do plano em `.specs/plano-sanitizer-alta-fidelidade.md`. Em `validate_bot_against_plan` (linha 349): troque a comparação de igualdade posicional total por: (1) `EXTRA_STEPS` = ids no código fora do conjunto de TODOS os ids do plano (required+optional+skip) — preserve a tolerância de `required_reopen` já existente (linhas 383-397), e ao decidir se um id 'extra' na posição de reopen é tolerado ou não, o conjunto de referência (`planned_set_for_reopen`) deve incluir TODOS os ids do plano, inclusive `skip` — isso é necessário para o caso em que a LLM reintroduz um `sup_NNN` legítimo nessa posição; (2) `MISSING_STEPS` = ids com hint ausente/`'required'` que não aparecem no código; (3) ordem: primeiro filtre para `code_ids_in_plan = [cid for cid in code_ids if cid in emit_allowed]` (achado da 3ª revisão independente, 2026-07-11 — um id de código que NÃO está no plano — extra/alucinado pela LLM, o cenário MAIS COMUM de erro deste pipeline, não uma raridade — nunca participa do cálculo de ordem; ele já foi reportado em `EXTRA_STEPS` no item (1) acima; mapear `code_id → índice no plano` com um lookup por valor tipo `list.index()` SEM filtrar antes faz um id extra lançar `ValueError` não tratado, que sobe a pilha e vira `RuntimeError` fatal em `code_generator.py`, abortando o Ralph Loop inteiro em vez de só reportar `EXTRA_STEPS` como o código atual sempre fez); só então, sobre essa subsequência filtrada, mapeie cada id restante para seu índice no plano e exija sequência estritamente crescente — mantenha o TIPO de erro `STEP_ID_MISMATCH` (NÃO renomear para `STEP_ORDER_VIOLATION` nem qualquer outro nome — achado da 2ª revisão independente, 2026-07-11: `code_generator.py:603` dispara a reordenação automática determinística checando literalmente `error_types.issubset({"STEP_ID_MISMATCH"})`; renomear quebraria esse gatilho para TODOS os projetos, não só os re-sanitizados em v2, pois este validador é compartilhado — o mesmo literal também aparece no prompt em `code_generator.py:1456` e no comentário `1353-1356`, que não estão no escopo desta tarefa e não devem ser tocados); só a LÓGICA de detecção muda (de igualdade posicional estrita para subsequência monotônica), tipo e formato da mensagem ficam idênticos ao atual — preserve as chaves `position`/`expected_id`/`found_id`/`detail` no dict de erro, mesmo formato do código atual (linhas 424-431): `code_generator.py:1379-1382` lê `expected_id`/`found_id` desses erros para decidir o escopo da correção cirúrgica, então omitir essas chaves degrada esse direcionamento mesmo sem quebrar a validação; (4) `COUNT_MISMATCH` compara só o subconjunto required. Em `validate_resilience_patterns` (linha 732): adicione, IMEDIATAMENTE ANTES do loop `for step in plan.get('steps', []):` (linha 834), `code_ids = set(extract_step_ids_from_code(bot_code))`; logo no topo do CORPO desse loop (antes do primeiro `if step_type ==`), adicione `if step.get('execution_hint') in ('optional', 'skip') and step['step_id'] not in code_ids: continue`. Um plano v1 (sem nenhum `execution_hint`) deve produzir EXATAMENTE os mesmos erros, nos mesmos casos, que o código atual — essa é a checagem de retrocompatibilidade mais importante desta tarefa. Escreva `aegis_sanitizer/test_step_validator_hints.py` cobrindo: plano v1 sem hints reproduz os erros de hoje; matriz v2 (só required = PASS; optional emitido em ordem = PASS; skip emitido em ordem = PASS; ordem violada = FAIL; required faltando = FAIL; id inventado fora do plano = EXTRA_STEPS); e o caso específico de um `sup_NNN` reintroduzido pela LLM exatamente na posição de uma correção `required_reopen` — não pode ser tratado como id desconhecido. NÃO toque em `_strip_internal_step_fields`, `_build_scoped_edit_plan`, `_render_plan_for_prompt` nem em qualquer coisa em `code_generator.py` — fora de escopo. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_step_validator_hints.py` passa.
  - [x] `python aegis_sanitizer/test_weak_selector_enforcement.py` continua passando (não depende de hints, mas exercita `validate_resilience_patterns`).

---

### [SUBAGENTE 06] - `code_generator.py` e playbook: contrato de fidelidade (T4)
> ✅ CONCLUÍDO — `_render_plan_for_prompt` nos 3 pontos, strip estendido, contrato no prompt + playbook; DoD revalidado manualmente pelo orquestrador (testes verdes + spot-check do render sem vazamento de campos internos).
- **🎯 Objetivo:** Ensinar o Code Generator (prompt + validador de campos internos + playbook) a lidar com steps `sup_`/`optional`: não emitir por default, poder reintroduzir com o `step_id` do plano quando necessário, nunca confundir bookkeeping interno com kwargs.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (Seção 6), saída de [SUBAGENTE 04] (schema real do plano v2)
  - Modificar: `aegis_sanitizer/code_generator.py` (`_strip_internal_step_fields`, linha 174; construção de `plan_steps_json`/`plan_slice_json`, linhas 826-831, 1148 e 1329-1334; blocos de instrução do prompt onde essas variáveis são injetadas, linhas ~985 e ~1439), `aegis_mentor/skills/rpa-copilot-coder.md`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é adaptar `aegis_sanitizer/code_generator.py` e o playbook `aegis_mentor/skills/rpa-copilot-coder.md` ao schema v2 do plano de execução, seguindo a Seção 6 de `.specs/plano-sanitizer-alta-fidelidade.md`. Em `_strip_internal_step_fields` (linha 174): estenda `internal_fields` para incluir também `merged_from`, `source_events`, `original_index`, `reordered_from`, `superseded_by` (bookkeeping puro que colide com nome de kwarg ou não deve ser manipulado pela LLM) — mantenha visíveis `execution_hint`, `step_role`, `suppression_reason`, `sanitization_notes`, `scenario`, `text`, `selector_original`, `has_text_original`. NÃO adicione `position_anchor` nem `dedup_group` a essa lista: `position_anchor` é variável interna do Sanitizer (Seção 8/T2 do plano) que nunca chega a ser serializada num step — não há nada pra stripar; `dedup_group` é um campo que não existe em nenhum lugar do schema real (achado da 2ª revisão independente, 2026-07-11) — se você encontrar `dedup_group` em qualquer versão anterior deste código/instrução, é resíduo a ignorar, não a replicar. Existem TRÊS pontos de renderização do plano no prompt, não dois (achado da 2ª revisão independente): `code_generator.py:826-831` (`plan_steps_json`, fluxo de geração nova, dentro de `_generate_new_code`), `:1329-1334` (`plan_steps_json`, fluxo cirúrgico full-file, dentro de `_surgical_correct`) e `:1148` (`plan_slice_json`, fluxo cirúrgico ESCOPADO — **correção da 3ª revisão independente, 2026-07-11: a linha 1148 fica dentro de `_surgical_correct_scoped`, não de `_build_scoped_edit_plan`** — `_build_scoped_edit_plan`, linhas 1078-1121, só localiza blocos de código por `step_id`, nunca recebe `plan_steps`; é `_surgical_correct_scoped`, que recebe `plan_steps` como parâmetro e consome o `scoped_plan` já produzido, quem monta `plan_slice_json`). Crie uma função `_render_plan_for_prompt(self, steps: list) -> str` e use-a nos TRÊS pontos: ela deve serializar em JSON completo (pós-`_strip_internal_step_fields`) os steps com `execution_hint` ausente/`'required'`/`'optional'`, e agrupar os steps `'skip'` numa seção de texto compacta separada, 1 linha por step (`step_id`, `type`, `selector` resumido, `suppression_reason`), sob um cabeçalho tipo `## PASSOS SUPRIMIDOS — contexto de fidelidade (não emitir por padrão)`. EXCEÇÃO no ponto `:1148` (modo escopado): o `plan_slice` já é filtrado antes por `target_step_ids` explícitos (só os blocos que a correção pede pra tocar, mais o contexto imediatamente antes/depois) — um `sup_` só deve aparecer nesse slice se ele PRÓPRIO for um dos `target_step_ids` (ex.: a LLM está reintroduzindo aquele suprimido especificamente); não adicione automaticamente outros `sup_` como 'contexto de fidelidade' nesse modo, ao contrário dos outros dois pontos. Adicione, nos dois blocos de instrução do prompt onde o plano é injetado (próximo às linhas ~985 e ~1439), um parágrafo curto explicando o contrato: passos 'skip' não devem ser emitidos por padrão; emita um passo suprimido SOMENTE se uma correção pendente ou o contexto do fluxo exigir (ex.: fechar overlay, re-disparar validação) e, ao emitir, USE o `step_id` exato do plano (nunca invente um novo) e preserve a ordem relativa; passos 'optional' ficam a critério da LLM, com justificativa em comentário no código gerado. Espelhe esse mesmo contrato como uma nova seção em `aegis_mentor/skills/rpa-copilot-coder.md`. NÃO toque em `_build_scoped_edit_plan`, `_surgical_correct_scoped` além do necessário para chamar `_render_plan_for_prompt` no lugar da serialização atual — o resto da lógica de correção cirúrgica fica igual. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_error_selector_config.py` passa.
  - [x] `python aegis_sanitizer/test_dryrun_multirow.py` passa.
  - [x] Verificação manual: gerar (ou reusar de teste) um `plano_execucao.json` v2 de fixture com pelo menos 1 step `skip`, chamar `_render_plan_for_prompt` diretamente (script ad-hoc) e confirmar visualmente que a seção `## PASSOS SUPRIMIDOS` aparece compacta (1 linha/step) e que nenhum campo de `internal_fields` vaza no JSON dos steps emitíveis.

---

### [SUBAGENTE 07] - Relatório com fidelidade (T5)
> ✅ CONCLUÍDO (2ª rodada) — 1ª rodada achou falha de DECOMPOSIÇÃO no DoD original (os 4 suprimidos do projeto de referência são nível-STEP — `phantom_click`/`superseded_correction`, criados em `_write_execution_plan` — e nunca recebem `sanitizer_class` no evento bruto; o DoD amarrava o badge só ao nível-evento R1-R4, que nesse dataset dá 0). Orquestrador corrigiu o escopo: relatório agora expõe as DUAS camadas — badge nível-evento no fluxo + seção nova `## 🔇 Passos Suprimidos no Plano de Execução` (lida do plano_execucao.json gravado no mesmo fluxo) + cabeçalho de duas camadas. Revalidado manualmente: grep SUPRIMIDO = 4, golden_diff continua OK, suítes verdes.
- **🎯 Objetivo:** `relatorio.md` passa a mostrar eventos suprimidos com badge em vez de silenciá-los, e a contagem de ações distingue total gravado × emitível.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (Seção 8/T5), saída de [SUBAGENTE 03]/[SUBAGENTE 04] (campos `sanitizer_class`/`execution_hint` disponíveis nos eventos/steps)
  - Modificar: `aegis_sanitizer/sanitizer.py` (seção de geração do `relatorio.md` dentro de `sanitize()`, a partir da linha ~289 — bloco `markdown.append(...)`)
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é atualizar a geração de `relatorio.md` dentro de `SanitizerService.sanitize()` (bloco de `markdown.append(...)` a partir da linha ~289 de `aegis_sanitizer/sanitizer.py`) para refletir eventos suprimidos em vez de omiti-los silenciosamente. No cabeçalho do relatório (linha ~292, '* Total de Ações Gravadas'), distinga o total de eventos classificados do total de ações emitíveis (ex.: 'Total de Ações Gravadas: 58 (49 emitíveis, 9 suprimidas)'). Na seção '🗺️ Fluxo de Passos e Bifurcações por Cenário' (a partir da linha ~342), para cada evento cujo `sanitizer_class.keep == False`, em vez de pular/omitir, adicione uma linha na tabela com um badge `🔇 SUPRIMIDO (<motivo curto de sanitizer_class.reason>)` no lugar da coluna de tipo, mantendo seletor e descrição visíveis. ATENÇÃO — ORDEM É CRÍTICA: o bloco de dedup de exibição já existente (linhas ~363-368) faz `continue` para cliques/fills consecutivos com o MESMO seletor do evento anterior — isso é EXATAMENTE a assinatura da regra R1 (`raw_duplicate_click`), então sem cuidado um evento R1 `keep:false` seria engolido por esse `continue` ANTES de chegar na lógica nova do badge, reintroduzindo a invisibilidade que esta tarefa existe para corrigir. Insira a checagem `sanitizer_class.keep == False` como a PRIMEIRA condição dentro do `if previous_event:` (antes dos dois `if`s de dedup consecutivo de hoje, linhas 364 e 366) — se o evento é suprimido, renderize a linha com o badge imediatamente e pule os dois `if`s de dedup consecutivo (não aplicam a um evento já classificado como ruído); só para eventos SEM `sanitizer_class.keep == False` o dedup consecutivo original (linhas ~364-368) continua rodando exatamente como hoje. NÃO mude a CONDIÇÃO desse dedup consecutivo (linhas ~363-368) — isso é sobre exibição consecutiva no markdown para eventos não-suprimidos, não sobre a classificação nova. NÃO toque em `_write_execution_plan` nem nos métodos de dedup de steps — fora de escopo. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] Copie `projects/portal_segura/tests/001_teste/` para uma pasta temporária (NUNCA rode contra o original — mesma regra de isolamento de [SUBAGENTE 01]/[SUBAGENTE 04]). Rode `python aegis_sanitizer/sanitizer.py --project-dir <pasta_temporaria>` e confirme `grep -c "SUPRIMIDO" relatorio.md` > 0. (Nota da execução: DoD original amarrava a supressão a `sanitizer_class`/nível-evento — premissa errada para este dataset; escopo corrigido pelo orquestrador para cobrir também nível-step lido do plano. Resultado final: grep = 4.)
  - [x] Leitura visual do `relatorio.md` gerado: contagem do cabeçalho consistente com `fidelity_summary` do `plano_execucao.json` correspondente (cabeçalho de duas camadas: supressão bruta × supressão de plano).

---

### [SUBAGENTE 08] - Correção da reconciliação de passos na Cockpit UI (T5b)
> ✅ CONCLUÍDO — reconciliação por Set de step_id com guard `hasStStepIds` + fallback posicional legado; harness Node do subagente (3 cenários) + replay independente do orquestrador com step_ids reais do historico_passos.json (cenários A e B OK, 0 falsos STOPPED).
- **🎯 Objetivo:** Corrigir `aegis_cockpit/static/index.html` para não duplicar `st_` bem-sucedidos como `STOPPED` quando o plano v2 tem `sup_` intercalados — bug real confirmado, não cosmético (achado do plan-critic).
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (Seção 7 e T5b), `aegis_cockpit/static/index.html` (linhas 3705-3792, construção de `robotSteps` e a reconciliação por posição)
  - Modificar: `aegis_cockpit/static/index.html` (bloco de reconciliação, linhas 3760-3792)
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é corrigir o bloco em `aegis_cockpit/static/index.html`, linhas 3760-3792, que hoje faz `const executedCount = robotSteps.length; if (planSteps.length > executedCount) { for (let i = executedCount; i < planSteps.length; i++) { ... status: 'STOPPED' ... } }` — essa fatia POSICIONAL assume que `robotSteps` (histórico real de execução, um item por `step_id` executado, já construído acima nas linhas 3731-3758) é sempre um prefixo exato de `planSteps` (`executionPlan.steps`). Isso deixa de valer quando o plano tem steps `sup_` intercalados (schema v2): mesmo uma execução 100% bem-sucedida acaba duplicando um `st_` já concluído como `STOPPED` na tela. Troque a fatia posicional por reconciliação via conjunto de `step_id`: construa `const executedIds = new Set(robotSteps.map(s => String(s.index)));` e, iterando `planSteps` na ordem, só empurre uma entrada `STOPPED` para steps cujo `execution_hint` esteja ausente ou seja `'required'` E cujo `step_id` não esteja em `executedIds`. Steps com `execution_hint === 'optional'` ou `'skip'` ausentes de `executedIds` são o caso esperado (não foram emitidos pelo bot) — NÃO geram entrada `STOPPED`. Preserve o formato do objeto empurrado (mesmos campos: `index`, `exec_order`, `type`, `selector`, `desc`, `status`, `error`, `usedHealing`, `screenshot`, `row_id`, `event_index`, `flaky`) e a lógica de guarda `alreadyMerged` (linha 3769) exatamente como está. CUIDADO ADICIONAL (achado da 2ª revisão independente, 2026-07-11): `robotSteps` é construído acima (linha 3743) com `const stepId = s.step_id !== undefined && s.step_id !== null ? s.step_id : (s.index || idx + 1);` — uma execução LEGADA sem `step_id` gravado no histórico produz `stepId` numérico puro (`1`, `2`, ...), que NUNCA vai bater com as strings `"st_NNN"` do plano dentro de `executedIds`. Se você aplicar a reconciliação nova cegamente nesse caso, o plano INTEIRO seria marcado `STOPPED` por engano. Antes de aplicar a lógica nova, verifique se `robotSteps` tem pelo menos uma entrada com `typeof s.index === 'string' && s.index.startsWith('st_')` — só então use `executedIds`/reconciliação por `step_id`; caso contrário (histórico legado, sem nenhum `step_id` string), mantenha o comportamento posicional ANTIGO (`planSteps[robotSteps.length..]`) como fallback, exatamente como está hoje. NÃO toque em nenhum outro trecho de `index.html` fora dessas linhas 3760-3792. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] Verificação via harness Node (extraído verbatim do bloco real) no lugar do navegador: histórico 100% bem-sucedido (63 step_ids reais) + plano v2 com `sup_` intercalados — nenhum `st_` bem-sucedido duplicado/`STOPPED` (0 falsos positivos). Validação visual no navegador fica coberta por [SUBAGENTE 10]/T7.
  - [x] Execução interrompida (40/63): os 23 `st_` `required` não alcançados viram `STOPPED`; nenhum `sup_`/`skip` empurrado.
  - [x] Histórico LEGADO (índices numéricos) — cai no fallback posicional antigo, não marca o plano inteiro como `STOPPED`.

---

### [SUBAGENTE 09] - Adaptação da suíte existente (T6)
> ✅ CONCLUÍDO — NENHUMA edição foi necessária: os 12 testes de test_sanitizer_execution_plan.py já passam contra o schema v2 (inclusive o dropdown-collapse), weak_selector 5/5, runner_integration 42/42; invariante `position_anchor` nunca-persistido confirmado estática e dinamicamente. Revalidado manualmente pelo orquestrador.
- **🎯 Objetivo:** Ajustar o único teste que genuinamente diverge por causa de campos aditivos do schema v2, e confirmar (sem editar) que o resto da suíte sanitizer/runner segue verde.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (T6)
  - Modificar: `aegis_sanitizer/test_sanitizer_execution_plan.py` (apenas o teste `test_dropdown_pair_with_fallbacks_collapses_without_fallback_field`)
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é rodar `python aegis_sanitizer/test_sanitizer_execution_plan.py` contra o `aegis_sanitizer/sanitizer.py` já reescrito (por uma tarefa anterior deste backlog) e ajustar SOMENTE as asserções do teste `test_dropdown_pair_with_fallbacks_collapses_without_fallback_field` que falharem por causa de campos aditivos do schema v2 (ex.: `step_role`, `source_events` — **NÃO `position_anchor`: corrigido na 3ª revisão independente, 2026-07-11, esse campo é variável LOCAL do algoritmo de interleave da Seção 8/T2 e NUNCA é persistido em nenhum step do JSON de saída — se `position_anchor` aparecer em qualquer lugar do output, é bug de [SUBAGENTE 04], não campo aditivo esperado a tolerar aqui**) presentes no step colapsado — a asserção deve continuar confirmando que o colapso produz 1 único step emitível do tipo `'select'` e que ele NÃO carrega `fallback_selectors`, só tolerando os campos novos aditivos. Rode a suíte completa e reporte, para CADA outro teste do arquivo que falhar, o motivo — se algum outro teste falhar, NÃO o edite: pare e reporte como possível regressão real em vez de ajustar a asserção para passar (lição registrada em CLAUDE.md, 'Working Agreements', item 3 — regressão de teste não é automaticamente bug do teste). NÃO toque em `test_weak_selector_enforcement.py` nem em qualquer outro arquivo de teste. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_sanitizer_execution_plan.py` — 100% dos testes passam (12/12, sem nenhuma edição no arquivo de teste).
  - [x] `python aegis_sanitizer/test_weak_selector_enforcement.py` passa (inalterado, 5/5).
  - [x] `python aegis_runner/test_runner_integration.py` passa (inalterado, 42/42 — confirma que a mudança de schema não quebrou o consumo do `flaky_step_ids` pelo runner).

---

### [SUBAGENTE 10] - Validação de ponta a ponta real (T7 — working agreement nº 1)
> ✅ CONCLUÍDO — golden_diff OK (63 st_ + 4 sup_, só-adição); regression gate APROVADO (3 runs, 0% sucesso = idêntico ao baseline pré-existente documentado, nenhuma regressão nova); Ralph Loop passou na 1ª tentativa na regeneração, zero `sup_` vazado no bot, execução real com taxa ≥ baseline. Pasta de validação apagada. Revalidado pelo orquestrador ao longo da execução (mtime/conteúdo, já que `projects/*` é gitignored).
- **🎯 Objetivo:** Provar, contra código real e (quando aplicável) navegador real, que o schema v2 completo (Sanitizer + validador + Code Generator + Cockpit UI) não regride o comportamento do bot compilado e que a geração respeita o contrato de fidelidade.
- **📂 Escopo de Arquivos:**
  - Ler: TODOS os arquivos modificados por [SUBAGENTE 03]-[SUBAGENTE 09]; `projects/portal_segura/tests/001_teste/*`
  - Modificar: nenhum arquivo de código-fonte. Só gera artefatos de execução dentro de uma CÓPIA de `projects/portal_segura/tests/001_teste/` (nunca no projeto original).
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é validação, não codificação — não edite nenhum arquivo em `aegis_*`. Passo 1: re-sanitize o projeto de referência real rodando `python aegis_sanitizer/sanitizer.py --project-dir projects/portal_segura/tests/001_teste` com a variável de ambiente `AEGIS_COGNITIVE_ENABLED` setada como `false` só para esta chamada (sintaxe depende do shell: PowerShell `$env:AEGIS_COGNITIVE_ENABLED='false'; python ...`; bash `AEGIS_COGNITIVE_ENABLED=false python ...`) — ATENÇÃO: `AEGIS_COGNITIVE_ENABLED=true` está setado tanto no `.env` raiz quanto no `.env` do projeto; sem forçar `false` nesta chamada específica, `refine_semantics_with_llm` reescreve `business_description` de TODOS os eventos a cada execução (não-determinístico), poluindo o diff do Passo 1 com mudanças de texto sem relação com o schema v2 e dificultando confirmar visualmente que é "só adição" — desligar aqui é só para limpar ESTE diff de verificação; o Passo 3 (regeneração real da Fase 4) deve usar o `.env` do projeto tal como está, sem forçar nada, pois ali o objetivo é validar o comportamento real de produção. Este é o único passo do projeto de referência ORIGINAL que efetivamente modifica o projeto em disco (propositalmente — é o que está sendo validado); mostre o diff do `plano_execucao.json` resultante contra a versão em git (`git diff -- projects/portal_segura/tests/001_teste/plano_execucao.json`) — confirme que é só ADIÇÃO de campos/steps `sup_`, sem nenhum `st_` deslocado, removido ou com `description`/`selector`/`type` alterado. Passo 2: invoque a skill `aegis-regression-gate` (ela já usa `portal_segura/tests/001_teste` como referência por padrão, conforme CLAUDE.md) e reporte o veredito literal (APROVADO/REPROVADO) — se REPROVADO, pare aqui e reporte, não tente corrigir. Passo 3, só se o Passo 2 for APROVADO: copie `projects/portal_segura/tests/001_teste/` inteiro para uma pasta de trabalho separada (ex.: `projects/portal_segura/tests/001_teste_v2_validation/`, ou pasta fora do repo), rode `python aegis_sanitizer/code_generator.py --project-dir <copia>` para regenerar o `bot_producao.py` com o plano v2, e então `python <copia>/bot_producao.py` contra o site real configurado no `.env`/`project.json` do projeto (mesma URL já usada por esse projeto — não é um site novo, não precisa perguntar URL ao usuário). Reporte: taxa de sucesso da execução, se o validador (`step_validator.py`) aceitou o código gerado sem erro, e se algum step `sup_` foi emitido pela LLM sem necessidade aparente (contrato do prompt violado). Ao final, apague a pasta de trabalho copiada — NUNCA deixe artefatos de teste dentro de `projects/portal_segura/tests/001_teste/` original."
- **🧪 Critério de Validação (DoD):**
  - [x] Diff do plano re-sanitizado (gerado com `AEGIS_COGNITIVE_ENABLED=false`, ver Passo 1) é só-adição (nenhum `st_` existente mudou de id/seletor/tipo/descrição). `golden_diff.py` exit 0.
  - [x] Veredito da skill `aegis-regression-gate` é APROVADO. 3 execuções, 0/1 sucesso em todas — idêntico ao baseline pré-existente (mesma classe de falha de self-healing/flakiness, documentada desde antes deste refactor, não é regressão nova). `correcoes_acumuladas.json` estável em 23 entradas nas 3 execuções.
  - [x] Execução real da cópia contra o site tem taxa de sucesso ≥ baseline (0% = 0%), nenhum step `sup_` emitido sem necessidade (grep confirmou zero ocorrências), Ralph Loop passou na 1ª tentativa.

---

### [SUBAGENTE 11] - Documentação (T8)
> ✅ CONCLUÍDO — 5 arquivos atualizados (architecture manual, walkthrough, CLAUDE.md, 2 mirrors da skill forense), todos aditivos, coexistindo com reescritas locais pendentes já existentes nos 2 docs. Revalidado pelo orquestrador: diff limpo, mirrors da skill forense idênticos em conteúdo (diferença de CRLF/LF pré-existente entre `.claude/` e `.agents/`, não é defeito), conteúdo novo confirmado nos 5 arquivos.
- **🎯 Objetivo:** Atualizar a documentação do framework para refletir o schema v2 e o novo contrato de fidelidade, coordenando com as edições locais já pendentes nesses arquivos.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-sanitizer-alta-fidelidade.md` (documento completo, já com as correções do plan-critic), resultado de [SUBAGENTE 10] (comportamento validado de verdade)
  - Modificar: `docs/aegis_architecture_manual.md`, `docs/aegis_rpa_suite_walkthrough.md`, `CLAUDE.md` (seção do Sanitizer), `.claude/skills/aegis-pipeline-forensics/SKILL.md`, `.agents/skills/aegis-pipeline-forensics/SKILL.md`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é atualizar a documentação do framework para descrever o schema v2 do `plano_execucao.json` (dois espaços de id `st_`/`sup_`, campo `execution_hint`, `fidelity_summary`) e o novo contrato de fidelidade do Sanitizer (classifica em vez de deletar). ATENÇÃO: `docs/aegis_architecture_manual.md` e `docs/aegis_rpa_suite_walkthrough.md` já têm modificações locais não commitadas neste repositório (rode `git diff -- docs/aegis_architecture_manual.md docs/aegis_rpa_suite_walkthrough.md` ANTES de editar, para não sobrescrever ou conflitar com esse trabalho em andamento) — integre suas edições rio abaixo do que já está lá, não substitua. Em `CLAUDE.md`, atualize a seção que descreve `aegis_sanitizer/sanitizer.py` (procure a tabela de 'Core Modules (Design-Time)' e a seção 'Resilience Catalog') para mencionar o novo paradigma de classificação. Em `.claude/skills/aegis-pipeline-forensics/SKILL.md` E seu espelho `.agents/skills/aegis-pipeline-forensics/SKILL.md` (mantenha os dois arquivos idênticos, como já são hoje), adicione uma nota sobre os novos campos forenses disponíveis na cadeia de artefatos: `sup_`, `merged_from`, `original_index`, `sanitizer_class`. NÃO reescreva seções inteiras dos documentos — edições cirúrgicas, aditivas. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `git diff` dos 5 arquivos mostra só adições/edições localizadas, sem remoção do conteúdo pré-existente não relacionado.
  - [x] `.claude/skills/aegis-pipeline-forensics/SKILL.md` e `.agents/skills/aegis-pipeline-forensics/SKILL.md` continuam idênticos entre si em conteúdo (diferença só de CRLF/LF, convenção pré-existente entre os diretórios `.claude/`/`.agents/`).

---

## 🗺️ Mapa de Dependências dos Subagentes

- 🟢 **Fase 1 (paralelo, somente-leitura/fixture, sem gate):** [SUBAGENTE 01] (T0a) e [SUBAGENTE 02] (T0b) — arquivos disjuntos, nenhum toca código de produção.
- 🚪 **Gate manual:** segunda revisão independente de `plan-critic` sobre o plano ajustado (ver nota no topo do arquivo) — antes de despachar a Fase 2.
- 🟡 **Fase 2:** [SUBAGENTE 03] (T1) — depende de 01/02 já estarem commitados (dependência temporal: golden precisa ser do estado PRÉ-T1, não uma dependência de leitura de arquivo).
- 🟡 **Fase 3 (paralelo):** [SUBAGENTE 04] (T2, depende de 02 + 03) e [SUBAGENTE 05] (T3, depende só do schema já especificado no plano — pode rodar em paralelo com 04, arquivo disjunto).
- 🟡 **Fase 4 (paralelo, todos dependem de 04):** [SUBAGENTE 06] (T4), [SUBAGENTE 07] (T5), [SUBAGENTE 08] (T5b), [SUBAGENTE 09] (T6) — 4 arquivos disjuntos (`code_generator.py`+playbook / `sanitizer.py` relatório / `static/index.html` / `test_sanitizer_execution_plan.py`).
- 🔴 **Fase 5:** [SUBAGENTE 10] (T7) — depende de TODAS as anteriores (03-09); é o gate real contra navegador/site (working agreement nº 1 do CLAUDE.md).
- 🔵 **Fase 6:** [SUBAGENTE 11] (T8) — depende de 10 (documentação reflete comportamento já validado, não uma promessa).
