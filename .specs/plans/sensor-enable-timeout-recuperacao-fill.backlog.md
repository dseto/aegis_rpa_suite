# BACKLOG DE EXECUÇÃO - CLAUDE CODE

Origem: `.specs/plans/sensor-enable-timeout-recuperacao-fill.design.md` (revisado por `plan-critic` + `/reflect`). Cobre a Feature 1 (sensor `ENABLE_TIMEOUT` + recuperação por re-fill + item B recalibrado) e a Feature 2 (log de diagnóstico, sem gate). G não faz parte deste backlog (rejeitado no design).

Decomposição em 2 tarefas, não 3: a generalização das esperas de habilitação e a integração do sensor/recuperação em `click_resilient` são uma única unidade coerente (mesma função, ~450 linhas de contexto compartilhado) — separar isso em duas tarefas cold-agent criaria mais risco de integração do que benefício de paralelismo, já que são sequenciais por natureza (a integração precisa das esperas já generalizadas).

---

### [SUBAGENTE 01] - Buffer `self._recent_fills` em `fill_resilient`
> ✅ CONCLUÍDO. Re-verificado por mim: 37/37 testes, grep confirma append em exatamente 1 função (`fill_resilient`, 2 pontos: HUMAN_LIKE e DIRECT), blast radius limpo.

- **🎯 Objetivo:** Criar um buffer em memória (não persistido) que registra os últimos fills bem-sucedidos, pra servir de fonte de recuperação a uma feature futura (SUBAGENTE 02) — este bloco só cria e popula o buffer, não consome.
- **📂 Escopo de Arquivos:**
  - Ler: `aegis_runner/runner.py` (`__init__` por volta de `:97` para ver onde `steps_history` é inicializado e seguir o mesmo padrão; `fill_resilient` `:1585+`; `fill_human_like` `:1698+`, só pra confirmar que `fill_resilient` a chama internamente quando `strategy=="HUMAN_LIKE"`, ver `:1618-1622`)
  - Modificar: `aegis_runner/runner.py`, `aegis_runner/test_runner_integration.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é em `aegis_runner/runner.py`:
  >
  > 1. No `__init__` da classe `TransactionRunner` (perto de onde `self.steps_history = []` é inicializado, `:97`), adicionar `self._recent_fills = collections.deque(maxlen=30)` (import `collections` no topo do arquivo se ainda não existir).
  > 2. Em `fill_resilient` (`:1585+`), ao final de um preenchimento bem-sucedido (tanto no branch `strategy=="HUMAN_LIKE"` quanto no branch `DIRECT`), fazer `self._recent_fills.append({'selector': selector, 'text_val': text_val, 'strategy': strategy, 'step_id': step_id, 'target_description': target_description})`.
  > 3. **NÃO adicionar esse append dentro de `fill_human_like` (`:1698+`)** — `fill_resilient` já chama `fill_human_like` internamente quando `strategy=='HUMAN_LIKE'` (`:1618-1622`) e já faz seu próprio append conforme o passo 2; adicionar também dentro de `fill_human_like` duplicaria a entrada no buffer para todo fill HUMAN_LIKE.
  > 4. **NÃO adicionar esse append em `fill_chained` (`:1494+`).** `fill_chained` resolve via `parent`+`child` (locator hierárquico), não um seletor plano — o `selector_full` que ele monta pra log contém colchetes de `has_text` que não são sintaxe CSS válida (mesmo motivo já documentado em `click_chained`, `runner.py:1334-1338`). Uma entrada de `fill_chained` no buffer quebraria a recuperação (que vai chamar `fill_resilient(selector=...)` — incapaz de resolver esse formato). Fora de escopo deste plano — ver seção "Fora de escopo" do design de origem.
  > 5. Não conectar esse buffer a nenhuma lógica de recuperação nesta tarefa — isso é escopo de uma tarefa futura. Esta tarefa só cria e popula o buffer.
  > 6. Adicione um teste em `aegis_runner/test_runner_integration.py` (nova classe, ex. `TestRecentFillsBuffer`, seguindo o estilo das classes de teste já existentes no arquivo, ex. `TestClickNoEffectSensor`) que: (a) chama `fill_resilient` duas vezes com selectors diferentes e confirma que `runner._recent_fills` tem 2 entradas na ordem certa; (b) chama `fill_resilient` com `strategy='HUMAN_LIKE'` uma vez e confirma que `runner._recent_fills` tem exatamente 1 entrada (não 2) — este teste é o que prova que não há double-append; (c) chama `fill_resilient` 35 vezes seguidas e confirma que `len(runner._recent_fills) == 30` (cap do deque).
  > Não faça refatoração, renomeação nem melhoria fora deste objetivo. Não mexa em `click_resilient`, `_handle_click_failure`, `fill_chained` nem em nenhuma outra função de clique."
- **🧪 Critério de Validação (DoD):**
  - [ ] Rodar a suíte de integração: `python aegis_runner/test_runner_integration.py` — deve passar 100%, incluindo os 3 testes novos descritos acima.
  - [ ] Revisão manual: confirmar por grep (`grep -n "_recent_fills" aegis_runner/runner.py`) que o append acontece em exatamente 1 lugar (dentro de `fill_resilient`) — nunca em `fill_human_like` nem em `fill_chained`.

---

### [SUBAGENTE 02] - Sensor `ENABLE_TIMEOUT`: generalizar espera de habilitação + item B + recuperação por re-fill
> ✅ CONCLUÍDO. Re-verificado por mim: 40/40 testes (`test_runner_integration.py`), 7/7 (`test_cognitive_fallback.py`), `#btn-next-step` fora de `_CLICK_EFFECT_EXCLUDED_SELECTORS`, item B com timeout=500ms confirmado nos 2 pontos. Feature 2 (log de rede) foi pulada de propósito — `captured_network` não existe no runner em runtime, só no recorder — documentado no código em vez de inventar fonte de dado. Agente também corrigiu um bug de controle de fluxo descoberto durante a implementação (exceção terminal sendo recapturada e reiniciando o passo) via exceção marcadora interna `_ClickTerminalFailure` — revisado, escopo justificado (necessário pro item 3 funcionar certo).
>
> ⚠️ **Regressão achada no gate de aceitação (close-backlog) e corrigida fora do fluxo de subagente, diretamente pelo orquestrador:** `_recover_via_recent_fills` (`runner.py:1000-1012`) chamava `fill_resilient` sem checar se o campo ainda estava presente/visível na tela. Numa execução real do bot de referência (`portal_segura/tests/001_teste`), um `ENABLE_TIMEOUT` disparou e a recuperação tentou reencher entradas do buffer de uma tela já navegada — cada entrada stale pagava o timeout interno cheio de `fill_resilient` (5s, podendo escalar até fallback visual/cognitivo), minutos perdidos por disparo. Corrigido: checagem rápida (`is_visible(timeout=500)`) antes de cada re-fill, pula a entrada se não estiver mais visível. Teste de regressão novo: `test_stale_buffer_entry_is_skipped_not_full_timeout` (`TestEnableTimeoutSensor`). Re-verificado: 41/41 (`test_runner_integration.py`), 7/7 (`test_cognitive_fallback.py`). **Gate de regressão precisa rodar de novo com esse fix — a execução anterior rodou contra o código com o bug.**

- **🎯 Objetivo:** Fazer `click_resilient` detectar quando um clique depende de um alvo que nunca habilita (qualquer selector, não só os 2 hardcoded hoje) e, antes de fechar o passo como sucesso ou falha, recuperar re-preenchendo os fills recentes do buffer criado no SUBAGENTE 01 — fechando o falso-sucesso observado ao vivo no Portal Segura (campo Nome preenchido antes da busca assíncrona de CPF terminar, botão `#btn-next-step` nunca habilita).
- **📂 Escopo de Arquivos:**
  - Ler: `aegis_runner/runner.py` inteiro é grande demais — focar em: `click_resilient` (`:444-607`, especialmente os force-clicks em `:525` e `:556` e as chamadas a `_wait_if_wizard_transition_button` em `:526`/`:595`), `_wait_for_known_disabled_button`/`_wait_if_wizard_transition_button` (`:823-870`), `_finalize_click_success` (`:608+`), `_handle_click_failure`/`_attempt_deterministic_click_recovery`/`_handle_unrecoverable_click` (`:652-822` aprox., pra reusar o mesmo tratamento de `strict`/flaky/cognitivo), `_CLICK_EFFECT_EXCLUDED_SELECTORS` (`:331`), `_register_healing_for_review` (`:228`), `self._recent_fills` (criado no SUBAGENTE 01 — bloqueante, precisa estar pronto antes desta tarefa)
  - Modificar: `aegis_runner/runner.py`, `aegis_runner/test_runner_integration.py`
- **Depende de:** [SUBAGENTE 01] concluído (consome `self._recent_fills`).
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é em `aegis_runner/runner.py`, dentro de `click_resilient` e das duas funções de espera de botão:
  >
  > 1. **Generalizar as esperas de habilitação.** `_wait_for_known_disabled_button` (`:823-844`) hoje só age se `selector in {'#btn-confirm-payment-progress'}`; `_wait_if_wizard_transition_button` (`:846-870`) hoje só age se `selector == '#btn-next-step'`. Remova essas restrições — as duas devem passar a agir para QUALQUER selector (mantendo o mesmo padrão de polling de 300ms e teto de 15s). Ambas devem passar a RETORNAR um booleano indicando se o elemento ficou habilitado dentro do prazo (hoje nenhuma das duas retorna nada).
  > 2. **Item B — tentativa de clique nativo antes do force-click.** No loop de candidatos de `click_resilient` (por volta de `:556`, onde hoje é `loc.click(timeout=3000, force=True)`), ANTES desse force-click, tente `loc.click(timeout=500, force=False)` dentro de um `try/except` que captura qualquer exceção e cai direto pro force-click existente (não propague a exceção da tentativa sem force). Mesma coisa no branch de `:525` (`page.locator(selector).click(timeout=timeout, force=True)`) — tentar sem force primeiro, timeout 500ms, catch e cair pro force. Não altere o comportamento em caso de sucesso da tentativa sem force (só pula o force-click seguinte).
  > 3. **Sensor `ENABLE_TIMEOUT` com recuperação.** Nos dois pontos onde `_wait_if_wizard_transition_button` é chamada hoje (`:526`, `:595`), use o retorno booleano do passo 1: se `False` (o elemento nunca habilitou), NÃO chame `_finalize_click_success` direto. Em vez disso, chame um novo método `_recover_via_recent_fills(page, selector, step_id)` que: (a) itera `self._recent_fills` do mais antigo pro mais recente; (b) para cada entrada, re-executa o fill com a MESMA `strategy` gravada (chamando `fill_resilient` de novo com os mesmos parâmetros — reusar a função existente, não duplicar lógica de digitação); (c) aguarda um settle curto (300-800ms, mesmo padrão de tolerância usado no sensor `CLICK_NO_EFFECT`) entre cada re-fill; (d) ao final, chama de novo a espera de habilitação generalizada do passo 1, uma única vez. Se essa segunda espera retornar `True`, prossiga para `_finalize_click_success` normalmente. Se ainda retornar `False`, trate como falha genuína do clique: delegue para o mesmo tratamento de `strict`/flaky/cognitivo que `_handle_unrecoverable_click` já implementa (reusar essa função, não duplicar a lógica de decisão strict/cognitivo/coordenadas).
  > 4. **Diagnóstico (Feature 2, log apenas, não decide nada).** Dentro de `_recover_via_recent_fills`, ao disparar a recuperação, inclua na chamada de log/registro de correção (mesmo padrão de `_register_healing_for_review` já usado em outros pontos de healing) uma nota com as entradas mais recentes de `self.captured_network` do recorder, se essa informação estiver disponível no contexto do runner — se não estiver disponível (ex.: o runner não tem acesso a `captured_network`, que é uma estrutura do recorder, não do runner), pule esse sub-item e diga isso explicitamente no resultado da tarefa em vez de inventar uma fonte de dados que não existe.
  > 5. **Reavaliar exclusão do `#btn-next-step`.** Depois de 1-3 implementados, remova `#btn-next-step` de `_CLICK_EFFECT_EXCLUDED_SELECTORS` (`:331`) — agora esse selector tem um sensor específico (`ENABLE_TIMEOUT`) cobrindo o caso de falso-sucesso, não faz sentido continuar excluído do sensor geral (`CLICK_NO_EFFECT`) também.
  > 6. Atualize os testes existentes que dependem do comportamento antigo (retorno mudo das 2 funções de espera, exclusão de `#btn-next-step`) em `aegis_runner/test_runner_integration.py` — ajuste as asserções para o novo contrato, não reverta a mudança pra fazer o teste antigo passar (mesmo princípio do Working Agreement #3 do `CLAUDE.md` do projeto). Adicione pelo menos um teste novo que reproduza o cenário motivador: um clique cujo alvo nunca habilita, com fills recentes no buffer, confirme que `fill_resilient` é chamado de novo pra cada entrada do buffer (mock) e que o passo só falha definitivamente se a segunda checagem de habilitação também falhar.
  > A recuperação do passo 3b só cobre entradas vindas de `fill_resilient` (é a única função que popula `self._recent_fills`, ver SUBAGENTE 01) — não precisa (nem deve) lidar com `fill_chained`. Não faça refatoração, renomeação nem melhoria fora deste objetivo. Não toque em `click_chained`, `fill_chained`, nem mude a assinatura de `fill_resilient`."
- **🧪 Critério de Validação (DoD):**
  - [ ] Rodar a suíte de integração: `python aegis_runner/test_runner_integration.py` — deve passar 100%, incluindo os testes ajustados e o teste novo do cenário motivador.
  - [ ] Rodar a suíte de fallback cognitivo: `python aegis_runner/test_cognitive_fallback.py` — deve passar 100% (a recuperação cai no mesmo caminho de decisão `strict`/cognitivo que já existe, não pode quebrar esse fluxo).
  - [ ] Revisão manual: confirmar por grep (`grep -n "_CLICK_EFFECT_EXCLUDED_SELECTORS" aegis_runner/runner.py`) que `#btn-next-step` não está mais no set.
  - [ ] Revisão manual: confirmar que a tentativa de clique sem force (item B) está com timeout de 500ms (ou próximo disso, não 1-2s) nos dois pontos (`:525`, `:556` originais).

---

## 🗺️ Mapa de Dependências dos Subagentes

- 🟢 Fase 1: [SUBAGENTE 01] — sem dependências, isolado (`fill_resilient` apenas — `fill_chained` fica fora de escopo).
- 🟡 Fase 2: [SUBAGENTE 02] — depende de [SUBAGENTE 01] (consome `self._recent_fills`). Não roda em paralelo com 01: ambos tocam `aegis_runner/runner.py`, e 02 precisa do buffer já existente e testado antes de escrever a lógica de recuperação que o consome.

---

> 🏁 DEMANDA FECHADA — 2026-07-10

## 🏁 Fechamento — Sensor `ENABLE_TIMEOUT` + recuperação por re-fill (Feature 1), diagnóstico de rede (Feature 2, escopo reduzido)

- **Aceitação:** ✅ suíte · ✅ integração · ✅ objetivo
- **Evidência:**
  - Suíte completa rodada pelo orquestrador (não pelo subagente): `python aegis_runner/test_runner_integration.py` (41/41), `python aegis_runner/test_cognitive_fallback.py` (7/7), `test_sanitizer_execution_plan.py` (12/12), `test_error_selector_config.py`, `test_weak_selector_enforcement.py` (5/5), `test_dryrun_multirow.py` (3/3) — 70 testes, 0 falhas.
  - Código conferido por grep/leitura direta (não só relato do subagente): `_CLICK_EFFECT_EXCLUDED_SELECTORS` (`runner.py:348`) contém só `#btn-confirm-payment-progress`; `_wait_for_known_disabled_button`/`_wait_if_wizard_transition_button` (`runner.py:903`, `:929`) generalizados para qualquer selector, retornam bool; `_recover_via_recent_fills` (`runner.py:991`) conectado nos dois call sites (`:557`, `:651`); checagem `is_visible(timeout=500)` presente (`runner.py:1042`) — cobre a regressão achada e corrigida durante o próprio close-backlog anterior (buffer stale causando cascata de timeouts).
  - Integração/comportamento real — **correção pós-`/reflect`**: a evidência citada originalmente (`run_20260710_084726`) foi verificada de novo e **não prova nada sobre este sensor** — grep em `execution.log` confirma zero ocorrência de `ENABLE_TIMEOUT` nesse run; os 4 `HEALED` ali são de causa diferente (Shadow DOM, PIX). Evidência correta, re-checada via grep direto no `execution.log` de cada run (não por inferência):
    - `run_20260710_084455`: `[AEGIS RUNNER] ⚠️ ENABLE_TIMEOUT | st_018 | #btn-next-step | ... reexecutando 6 preenchimento(s)...` → recuperação rodou → passo fechou `HEALED`. Ramo de **recuperação bem-sucedida**, real, ao vivo.
    - `run_20260710_065027`: mesmo disparo (`st_018`) → recuperação tentou e **falhou de verdade** (`FAILED ... ENABLE_TIMEOUT: elemento-alvo ... permaneceu desabilitado`) → cognitivo (`IA DIAGNOSE [BUSINESS_VALIDATION]`, chamada real de LLM, não mock) diagnosticou corretamente "Placa do Veículo/Número do Chassi vazios" — falha genuína de validação de negócio, não bug do framework. Ramo de **falha genuína tratada corretamente**.
    - Os dois ramos do design (recuperação resolve / recuperação não resolve → falha real) estão cobertos por evidência ao vivo real, com LLM de runtime de fato acionado no segundo caso.
  - Objetivo do plano: gap #1 (exclusão do `#btn-next-step` do `CLICK_NO_EFFECT`) e gap #2 (ausência de espera pré-clique genérica) fechados — confirmado por grep + pela suíte. Feature 2 (log de rede) foi conscientemente reduzida de escopo pelo subagente: `captured_network` não existe no runner em runtime (só no recorder), documentado no código (`runner.py:1022-1029`) em vez de inventar fonte de dado — aceitável, já que o design marca a Feature 2 como não-bloqueante.
  - **Fora de escopo confirmado, não testado neste ciclo:** geração de código e correção cirúrgica via LLM (Fase 4, `code_generator.py`) — este backlog só toca `aegis_runner/runner.py` (runtime), nunca `code_generator.py`; nenhum bot foi (re)gerado ou corrigido via LLM como parte desta demanda.
- **Fechamento:** `CLAUDE.md` atualizado com nova seção "Enable-Timeout Detection (`ENABLE_TIMEOUT`)" (documentava só `CLICK_NO_EFFECT` antes). Commit ainda não feito — aguardando aval do usuário para escopo e mensagem.
- **Achado durante a reflexão (não corrigido aqui, fora do escopo já fechado):** recuperação bem-sucedida via `_recover_via_recent_fills` cai em `_finalize_click_success` (`runner.py:561`) sem chamar `_register_healing_for_review` com um `healing_method` próprio (ex. `enable_timeout_recovered`) — só fecha como `HEALED` se o sensor `CLICK_NO_EFFECT` (mecanismo diferente) coincidentemente também detectar mudança. Contradiz o princípio já documentado no `CLAUDE.md` ("toda camada que resolve via healing auto-registra `needs_review`"). Aberto como tarefa separada, não implementado agora.
- **Veredito:** DEMANDA FECHADA (evidência corrigida)
