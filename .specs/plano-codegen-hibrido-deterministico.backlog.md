# BACKLOG DE EXECUÇÃO - CLAUDE CODE

**Plano de origem:** [`plano-codegen-hibrido-deterministico.md`](./plano-codegen-hibrido-deterministico.md) (3 rodadas de revisão em 2026-07-12 — plan-critic rodada 1, rodada 2 com agente frio independente testada contra planos reais de `projects/portal_segura/`, e re-checagem focada; todas as correções incorporadas no plano e sincronizadas neste backlog). Status do plano: **PRONTO PARA plan-to-backlog** — nenhuma re-revisão é exigida antes de despachar.

> **🔒 Working agreement do projeto (CLAUDE.md):** toda mudança em `aegis_*` core só é considerada concluída depois de rodar contra navegador real e passar pelo `aegis-regression-gate`. [SUBAGENTE 10] (H8) é o gate final disso — nenhuma tarefa anterior substitui essa validação. O flip do default `AEGIS_CODEGEN_HYBRID=true` só acontece DENTRO de [SUBAGENTE 10], condicionado ao veredito APROVADO.

> **⚠️ Conflito de arquivo — despacho sequencial obrigatório:** o plano permite {H3, H5} e {H6, H7} em paralelo, mas cada par modifica `aegis_sanitizer/code_generator.py`. Subagentes frios editando o mesmo arquivo em paralelo geram conflito de escrita. Este backlog serializa esses pares (Fases 4→5 e 7→8). Só paralelize se despachar em worktrees isoladas com merge manual.

> **📌 Premissa herdada do plano (Seção 0):** a "Mudança A" (schema v2, hints, `sup_`) **já está implementada e commitada** (`b5cc4e7`). Este backlog é SÓ a Mudança B (geração híbrida). Nenhuma tarefa aqui toca `aegis_runner/`, `aegis_blackbox/`, `aegis_sanitizer/sanitizer.py` nem `aegis_cockpit/` — se um subagente achar que precisa, é sinal de erro de rota: parar e reportar.

---

### [SUBAGENTE 01] - Baseline congelado + golden v2 rico (H0a)
> ✅ CONCLUÍDO — baseline em `.specs/baseline-codegen-hibrido.md` (hash `b5cc4e79f27fba399c66406201b5058cb03133a6`); golden v2 rico em `.specs/golden/real_portal_segura_001_v2/`. Células ausentes no golden real (escopo de [SUBAGENTE 02]): [a] optional, [d] weak_selector, [h] select_native.
- **🎯 Objetivo:** Congelar os artefatos de referência (bot compilado atual + goldens existentes + hash do commit) e capturar um `plano_execucao.json` v2 RICO re-sanitizando a gravação do golden real com o sanitizer v2 já commitado — insumo obrigatório do round-trip de [SUBAGENTE 04].
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/golden/real_portal_segura_001/gravacao.json`, `.specs/golden/real_portal_segura_001/dicionario.json`, `projects/portal_segura/tests/001_teste/code/bot_producao.py` (NÃO regenerar, NÃO modificar), `aegis_sanitizer/sanitizer.py` (só executar)
  - Criar: `.specs/golden/real_portal_segura_001_v2/plano_execucao.json`, `.specs/golden/real_portal_segura_001_v2/META.md`, `.specs/baseline-codegen-hibrido.md` (anotação de baseline)
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é congelar o baseline da demanda 'code generator híbrido' e capturar um golden v2 rico, sem alterar NENHUM código de produção. Passos: (1) Rode `git rev-parse HEAD` e anote o hash. (2) Escreva `.specs/baseline-codegen-hibrido.md` registrando: o hash; que o bot de referência é `projects/portal_segura/tests/001_teste/code/bot_producao.py` (compilado, congelado — NUNCA regenerar durante esta demanda); que os goldens pré-existentes são `.specs/golden/real_portal_segura_001/` (plano v1 puro, 63 steps, zero `execution_hint`, zero `sup_`) e `.specs/golden/synthetic_r1_merge_case/` (v2 mínimo, 2 steps). (3) Copie `.specs/golden/real_portal_segura_001/{gravacao.json,dicionario.json}` mais um `dataset_inicial.json`/`project.json` mínimos compatíveis (copie de `projects/portal_segura/tests/001_teste/` se a estrutura de pastas exigir) para uma pasta temporária descartável FORA de `projects/` (ex.: `.specs/golden/_tmp_v2_run/`), montando a estrutura de diretórios que `aegis_sanitizer/sanitizer.py --project-dir` espera (veja como o sanitizer resolve caminhos antes de montar). (4) Rode o sanitizer v2 atual contra essa pasta com a variável de ambiente `AEGIS_COGNITIVE_ENABLED=false` (PowerShell: `$env:AEGIS_COGNITIVE_ENABLED='false'; python aegis_sanitizer/sanitizer.py --project-dir <pasta>`) — obrigatório para a saída ser determinística: com gateway ativo, `refine_semantics_with_llm` reescreve descrições a cada execução. (5) Copie o `plano_execucao.json` resultante para `.specs/golden/real_portal_segura_001_v2/plano_execucao.json` e confirme que ele tem `\"version\": \"2.0\"`. (6) Escreva `.specs/golden/real_portal_segura_001_v2/META.md` com hash, data, comando exato (incluindo o env var) e a origem (`re-sanitização da gravacao.json do golden real com sanitizer v2, commit <hash>`). (7) Apague a pasta temporária. (8) Anexe ao `.specs/baseline-codegen-hibrido.md` um INVENTÁRIO da matriz de casos presente no plano v2 capturado — para cada caso, SIM/NÃO com o(s) step_id(s) de evidência: [a] step `optional`; [b] step com `parent`+`has_text`; [c] `select` com `original_coords_trigger`/`original_coords_option` (ou `coords`); [d] `weak_selector: true`; [e] `padrao_q` (step com `parent.has_text_original` presente); [f] click de opção de autocomplete com valor de negócio no seletor (forma do `st_023` real: seletor contendo `:has-text('<literal>')` cujo literal é um `observed_value` do `dicionario.json`, com `parent.has_text` nulo); [g] steps `sup_`/`skip`; [h] `select_native`. Esse inventário decide o escopo de [SUBAGENTE 02]. NÃO modifique `aegis_sanitizer/sanitizer.py` nem nada em `projects/`. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `.specs/golden/real_portal_segura_001_v2/plano_execucao.json` existe e `python -c "import json; p=json.load(open('.specs/golden/real_portal_segura_001_v2/plano_execucao.json', encoding='utf-8')); assert p['version']=='2.0', p['version']"` passa.
  - [x] `.specs/golden/real_portal_segura_001_v2/META.md` contém hash de commit válido (`git cat-file -e <hash>`).
  - [x] `.specs/baseline-codegen-hibrido.md` existe com o inventário da matriz (checklist [a]-[h], SIM/NÃO + step_ids de evidência).
  - [x] `git status` não mostra mudanças em `projects/` nem em `aegis_*/`.

---

### [SUBAGENTE 02] - Fixtures sintéticas para células vazias da matriz (H0b)
> ✅ CONCLUÍDO — `.specs/golden/synthetic_hybrid_matrix/` cobre [a] optional (st_001), [d] weak_selector (st_008/st_009, incl. negativo sem material), [h] select_native (st_004), + [b]/[c]/[e]/[f]/[g] e negativo C10 em st_006.
- **🎯 Objetivo:** Para cada célula da matriz [a]-[h] que o inventário de [SUBAGENTE 01] marcou como NÃO no golden v2 rico, criar uma fixture sintética mínima de plano v2 (+ dicionário), de forma que o round-trip de [SUBAGENTE 04] não tenha célula vazia.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/baseline-codegen-hibrido.md` (inventário de [SUBAGENTE 01]), `.specs/golden/real_portal_segura_001_v2/plano_execucao.json` (schema de referência), `.specs/golden/synthetic_r1_merge_case/plano_execucao_esperado.json` (exemplo de fixture v2 sintética), `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2.2 e 4.1), `projects/portal_segura/tests/001_teste/plano_execucao.json` e `dicionario.json` (formas reais dos campos)
  - Criar: `.specs/golden/synthetic_hybrid_matrix/plano_execucao.json`, `.specs/golden/synthetic_hybrid_matrix/dicionario.json`, `.specs/golden/synthetic_hybrid_matrix/README.md`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é criar fixtures de teste sintéticas, não código de produção. Leia o inventário da matriz em `.specs/baseline-codegen-hibrido.md`. Para cada caso [a]-[h] marcado NÃO no golden v2 real, adicione um step sintético mínimo a um único plano v2 de fixture `.specs/golden/synthetic_hybrid_matrix/plano_execucao.json` (schema v2: `version: '2.0'`, array `steps` com `step_id` `st_NNN`/`sup_NNN`, `execution_hint`, `step_role` quando aplicável, `fidelity_summary` consistente com as contagens), com um `dicionario.json` casado (campo `fields` com `selector`, chave semântica e `observed_value` onde o caso exigir binding). Casos que a fixture DEVE cobrir se faltarem no golden real: [a] step `execution_hint: 'optional'`; [b] click com `parent` (dict com `selector` e `has_text`) + `child`; [c] `select` customizado com `selector: \"\"`, `trigger_selector` preenchido e `original_coords_trigger`/`original_coords_option` — atenção: nos planos reais o binding do `select` vive em `trigger_selector`, não em `selector` (achado I2 da rodada 2 do plano); [d] step `weak_selector: true` COM material de ancoragem (`parent.has_text` ou campo `text`) e um segundo SEM material; [e] step com `parent.has_text_original` presente e diferente de `parent.has_text` + `sanitization_notes` com `padrao_q` (Padrão Q — sempre cognitivo); [f] click de opção de autocomplete com valor de negócio no seletor — forma exata do `st_023` real: `selector` tipo `#mat-autocomplete-panel-x div:has-text('<literal>')` onde `<literal>` é um `observed_value` do dicionário e `parent.has_text` é nulo (caso C10/B1 — a fixture deve forçar `classify_step` a retornar cognitive); [g] step `sup_NNN` com `execution_hint: 'skip'` e `step_role`/`suppression_reason`; [h] `select_native` com `selector` normal preenchido e SEM `trigger_selector` (nasce de fill em `<select>` — achado R1 da re-checagem: binda por `selector`, como fill). Se o inventário marcar tudo SIM, crie mesmo assim a fixture com os casos [d]-sem-material e [f] (são os testes negativos da linha de corte) e documente. Copie a FORMA dos campos dos steps reais de `projects/portal_segura/tests/001_teste/plano_execucao.json` — não invente nomes de campo. Escreva `README.md` mapeando cada step da fixture para a célula da matriz e para a condição C1-C10 da Seção 2.2 do plano que ele exercita, incluindo o resultado esperado de `classify_step` (deterministic/cognitive/omit) por step. NÃO crie nem modifique nenhum arquivo `.py`. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python -c "import json; json.load(open('.specs/golden/synthetic_hybrid_matrix/plano_execucao.json', encoding='utf-8')); json.load(open('.specs/golden/synthetic_hybrid_matrix/dicionario.json', encoding='utf-8'))"` passa.
  - [x] `README.md` tem a tabela step → célula da matriz → condição C → resultado esperado de `classify_step`, sem célula da matriz [a]-[h] descoberta (entre golden real v2 + esta fixture).
  - [x] `git status` não mostra mudanças fora de `.specs/golden/synthetic_hybrid_matrix/`.

---

### [SUBAGENTE 03] - `deterministic_emitter.py`: emissores puros (H1)
> ✅ CONCLUÍDO — `aegis_sanitizer/deterministic_emitter.py` + `test_deterministic_emitter.py` (26 testes). Nota técnica do subagente: usa `json.dumps(ensure_ascii=False)` para strings, nunca `repr()`, porque `_STEP_ID_IN_BLOCK_RE` (`code_generator.py:1127`) exige aspas duplas literais em `step_id="..."`.
- **🎯 Objetivo:** Criar o módulo novo `aegis_sanitizer/deterministic_emitter.py` com os emissores por tipo e `emit_step_block` — funções puras, zero integração com `code_generator.py` ainda.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2.1, 3.2), `aegis_sanitizer/step_validator.py` (função `validate_resilience_patterns`, linha 774 — os checks que cada emissor espelha), `projects/portal_segura/tests/001_teste/code/bot_producao.py` (forma real das chamadas runner geradas hoje), `projects/portal_segura/tests/001_teste/plano_execucao.json` + `dicionario.json` (formas reais dos steps/fields), `aegis_sanitizer/test_sanitizer_execution_plan.py` (convenção de teste do módulo)
  - Criar: `aegis_sanitizer/deterministic_emitter.py`, `aegis_sanitizer/test_deterministic_emitter.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é criar `aegis_sanitizer/deterministic_emitter.py`: Python puro (f-strings/builders), zero dependência nova, compatível `>=3.8`, sem Jinja. Nesta tarefa entram SOMENTE os emissores puros — `classify_step`/`build_skeleton`/manifest são de outra tarefa. Implemente: (1) `_emit_click(step)` — gera bloco `# [PASSO N] <description>` + chamada `runner.click_resilient(page, selector=..., target_description=step['description'], step_id=...)`; se o step tem `parent`, gera `runner.click_chained(page, parent={...}, child={...}, ...)` com `parent`/`child` como dicts literais (incluindo `has_text` do plano quando presente); se o step tem `coords`, inclui `original_coords={'x_percent': ..., 'y_percent': ...}`. Espelhe EXATAMENTE a forma que `validate_resilience_patterns` (`aegis_sanitizer/step_validator.py:774+`) cobra — leia os checks de click/chained/coords antes de escrever, e confira a assinatura real das chamadas no bot de referência `projects/portal_segura/tests/001_teste/code/bot_producao.py`. (2) `_emit_fill(step, field)` — `fill_resilient`/`fill_chained` com `text_val=row.get(\"<chave>\", \"\")` onde `<chave>` é a chave semântica do field do dicionário (resolução selector→chave é responsabilidade do chamador; `_emit_fill` recebe o field já resolvido); `strategy=\"HUMAN_LIKE\"` se `field.get('fill_strategy')` mandar, senão `\"DIRECT\"`; datas passam o valor de `row` direto, sem conversão (default seguro por construção). (3) `_emit_select(step, field)` — `select_option_resilient` com `option_text=row.get(\"<chave>\", \"\")` (NUNCA literal do plano — se não há chave o step nem deveria chegar aqui) + `original_coords_trigger`/`original_coords_option` quando presentes no step. (4) `_emit_select_native(step, field)` — `select_option_native_resilient`, mesma regra. (5) `_emit_async_guard(step, field)` — retorna `time.sleep(2.0)` pós-fill quando a chave semântica do field casa o regex `r\"cpf|cnpj|cep\"` (mesma heurística da regra 8 do prompt atual, agora determinística), senão string vazia. (6) `_emit_optional_wrapper(inner, step)` — envelopa o bloco em try/except não-fatal, template canônico da Seção 3.2 do plano: `try:` + corpo indentado + `except Exception as _opt_err:` + `print(f\"[BOT] Passo opcional <step_id> pulado (não-fatal): {_opt_err}\")` — o except SEMPRE imprime o erro. (7) `emit_step_block(step, dicionario)` — orquestra: resolve o field pelo binding por tipo (fill/select_native ⇒ `fields[*].selector == step['selector']`, fallback `selector_original`; select customizado ⇒ `fields[*].selector == step['trigger_selector']`), despacha para o emissor do tipo, aplica `_emit_async_guard` e, se `execution_hint == 'optional'`, o wrapper. Todo bloco emitido começa com o comentário-âncora `# [PASSO N] <descrição>` e toda chamada runner carrega `step_id=\"<step_id>\"` — âncoras são contrato do fluxo cirúrgico. Steps com `weak_selector: true` e material de ancoragem (`parent.has_text` OU campo `text`): componha o seletor com `:has-text(...)` na forma que `WEAK_SELECTOR_WITHOUT_ANCHOR` (`test_weak_selector_enforcement.py` mostra o contrato) aceita. Escreva `aegis_sanitizer/test_deterministic_emitter.py` na convenção do módulo (executável com `python aegis_sanitizer/test_deterministic_emitter.py`, mesmo padrão de imports de `test_sanitizer_execution_plan.py`), cobrindo por emissor: parent⇒chained com dicts; coords⇒original_coords; fill_strategy⇒HUMAN_LIKE vs DIRECT; weak+text⇒`:has-text`; select com coords_trigger/option; select_native binda por `selector` (SEM `trigger_selector` — os reais têm `trigger_selector: None`); async guard dispara para chave `cpf_cliente` e não dispara para `nome`; optional wrapper imprime o erro no except e o `Try` contém a chamada runner (verificável via `ast`). NÃO modifique nenhum arquivo existente. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_deterministic_emitter.py` passa.
  - [x] Suíte existente intocada e verde: `python aegis_sanitizer/test_sanitizer_execution_plan.py`, `python aegis_sanitizer/test_step_validator_hints.py`, `python aegis_sanitizer/test_weak_selector_enforcement.py`.
  - [x] `git status` mostra somente os 2 arquivos novos.

---

### [SUBAGENTE 04] - `classify_step` + `build_skeleton` + manifest + round-trip (H2 — gate central)
> ✅ CONCLUÍDO — 53 testes, round-trip zero erros nos 5 validadores + dry_run contra golden v1, v2 rico e fixture da matriz. Confirmado: `st_023`/`st_024`/`st_025` (autocomplete real) roteiam para cognitive via C10. Sem divergência emissor×validador — nenhuma edição em `step_validator.py`/`code_generator.py`. Assinatura estendida (documentada): `classify_step(step, dicionario, pending_corrections, next_step)` — `next_step` necessário para o lookahead da C9.
- **🎯 Objetivo:** Implementar a linha de corte C1-C10, o `build_skeleton` com placeholders cognitivos parseáveis e o manifest com `plan_checksum`; provar tudo com o teste de round-trip contra os goldens v1, v2 rico e a fixture sintética da matriz.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2.2, 2.3, 2.4, 4.1), `.specs/golden/real_portal_segura_001/plano_execucao.json` (v1), `.specs/golden/real_portal_segura_001_v2/plano_execucao.json` (v2 rico, de [SUBAGENTE 01]), `.specs/golden/synthetic_hybrid_matrix/*` (de [SUBAGENTE 02]), `aegis_sanitizer/step_validator.py` (funções `validate_bot_structure`, `validate_bot_against_plan`, `validate_resilience_patterns`, `validate_dataset_field_names`, `dry_run_bot`, `reorder_steps_to_match_plan`), `aegis_sanitizer/code_generator.py` (SOMENTE leitura: `_normalize_boilerplate` linha 32, `_STEP_ID_IN_BLOCK_RE` linha 1127, `_parse_step_blocks`)
  - Modificar: `aegis_sanitizer/deterministic_emitter.py` (criado por [SUBAGENTE 03]), `aegis_sanitizer/test_deterministic_emitter.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é adicionar a `aegis_sanitizer/deterministic_emitter.py` (módulo já existente com os emissores puros): `EmissionDecision` (NamedTuple `kind: str` ∈ {'deterministic','cognitive','omit'}, `reason: str`), `classify_step(step, dicionario) -> EmissionDecision` e `build_skeleton(plan, dicionario) -> tuple[str, dict]`. LINHA DE CORTE (Seção 2.2 do plano — um step é deterministic SOMENTE se TODAS valem; qualquer dúvida ⇒ cognitive): **C1** `type` ∈ {`click`,`fill`,`select`,`select_native`}; `filechooser` e qualquer tipo desconhecido ⇒ cognitive. **C2** `execution_hint` ausente ou `'required'`; `'optional'` ⇒ cognitive (decisão de emitir é da LLM); `'skip'` ⇒ omit (nem placeholder). **C3** sem Padrão Q operacional: `parent.has_text_original` ausente (o campo vive ANINHADO em `parent`, não top-level) E nenhuma entrada de `sanitization_notes` com `padrao_q` referente a `parent.has_text` — notes `padrao_q` geradas para `fallback_selectors` NÃO contam (campo interno que o emissor nem emite). **C4** binding por TIPO resolve para exatamente UMA chave do `dicionario.json`: para `fill` E `select_native` ⇒ `fields[*].selector == step.selector` (fallback `selector_original`) — `select_native` nasce de fill em `<select>` e carrega `selector` normal SEM `trigger_selector`; para `select` customizado ⇒ `fields[*].selector == step.trigger_selector` — os selects reais têm `selector: \"\"` e o binding vive em `trigger_selector` (campo interno escondido da LLM pelo strip, mas o EMISSOR pode e deve usá-lo). NÃO existe casamento por `dropdown_label` (os fields não têm essa chave). Zero ou 2+ casamentos ⇒ cognitive. Steps `click` sem valor de negócio não passam por C4. **C5** se `weak_selector: true`: exige material de ancoragem mecânica (`parent.has_text` OU campo `text` do step); sem material ⇒ cognitive. **C6** selector não casa heurística de menu suspenso do Padrão N (`.sub-menu`, `.dropdown-menu`, `#menu-item-`); casou ⇒ cognitive. **C7** condição GLOBAL: `skills_used` vazio; com skills o ARQUIVO INTEIRO cai no fluxo full-LLM (isso é decidido pelo chamador na integração — `classify_step` não vê skills; documente no docstring de `build_skeleton`). **C8** step NÃO referenciado por nenhuma correção pendente: checar `c['step_id']`, `c['required_reopen']['after_step_id']` e o alvo de `required_wait`/`required_method` de toda correção em `pending_corrections` (passe `pending_corrections` como parâmetro de `classify_step`/`build_skeleton`); referenciado ⇒ cognitive. **C9** `fill` que precede autocomplete ⇒ cognitive: o step emitível imediatamente seguinte no plano é (a) `select`/`select_native` OU (b) `click` cujo seletor casa `autocomplete|mat-option|\\[role='option'\\]|#mat-autocomplete-panel-|listbox` (a forma REAL do padrão é fill→CLICK em painel, ex. st_022→st_023). Decisão deste backlog: implementar a via CONSERVADORA (cognitive), NÃO a alternativa 'emitir HUMAN_LIKE por construção' — política do plano é dúvida ⇒ cognitive; H8.2 valida contra site real. **C10** nenhum valor de negócio no SELETOR: o `selector` (e `child` derivado) não contém `:has-text(<literal>)` cujo literal case algum `observed_value` do `dicionario.json`, E o campo `text` do step também não casa nenhum `observed_value`; casou ⇒ cognitive (caso real: `st_023`, `#mat-autocomplete-panel-marca div:has-text('Hyundai')` com 'Hyundai' em `observed_value` de `marca_veiculo`). **`flaky` é pass-through:** não participa da classificação nem altera o bloco. Suporte os overrides `AEGIS_CODEGEN_FORCE_LLM_STEPS` (lista CSV de step_ids rebaixados a cognitive — leia via `os.getenv` dentro de `classify_step` ou receba como parâmetro resolvido pelo chamador; escolha uma via e documente). `build_skeleton`: monta o corpo de `execute_scenario_default` — blocos deterministic via `emit_step_block` (numeração `# [PASSO N]` sequencial, assert interno de step_id nunca repetido); blocos cognitive viram o placeholder PARSEÁVEL de 3 linhas: `# [PASSO N] <descrição>` + `# AEGIS_COGNITIVE_SLOT step_id=\"st_014\" motivo=\"<reason>\"` + `pass` — o `step_id=\"...\"` dentro do comentário DEVE satisfazer o regex existente `_STEP_ID_IN_BLOCK_RE` (`code_generator.py:1127`), que exige literalmente `step_id=\"...\"` no texto do bloco (achado I7: sem essa forma, todo slot cai silenciosamente em full-LLM); o `pass` mantém o arquivo sintaticamente válido; steps `skip`/`sup_` ⇒ nada. Boilerplate (header/`__main__`) NÃO é responsabilidade do emitter — `_normalize_boilerplate` continua dono. Retorna também o manifest (dict): `{'generator_version': 'hybrid-1', 'generated_at': <iso>, 'plan_checksum': <sha1 do conteúdo do plano_execucao.json usado>, 'steps': {step_id: {'provenance': 'deterministic'|'cognitive', 'reason': ..., 'block_sha1': <sha1 do texto do bloco — TELEMETRIA apenas, nenhuma lógica depende dele>}}}` — steps omit não entram em `steps`. A GRAVAÇÃO do manifest em disco é da integração (outra tarefa); aqui só o dict. TESTES (adicione a `test_deterministic_emitter.py` — este é o gate central, Seção 4.1 do plano): (RT1) round-trip: para `.specs/golden/real_portal_segura_001/plano_execucao.json` (v1), `.specs/golden/real_portal_segura_001_v2/plano_execucao.json` e `.specs/golden/synthetic_hybrid_matrix/plano_execucao.json`, rode `build_skeleton` com TODOS os steps FORÇADOS a deterministic onde possível (bypass de classificação via parâmetro de teste ou monkeypatch — steps genuinamente impossíveis, como Padrão Q sem chave, ficam de fora e são documentados), monte o arquivo completo, aplique `_normalize_boilerplate` (importe de `code_generator`) e exija ZERO erros em `validate_bot_structure`, `validate_bot_against_plan`, `validate_resilience_patterns`, `validate_dataset_field_names` E `dry_run_bot`; (RT2) fixture com step `optional` emitido via wrapper ⇒ mesmos validadores PASS + `reorder_steps_to_match_plan` sobre versão embaralhada move o `Try` como unidade sem quebrar sintaxe (`ast.parse` no resultado); (RT4) o step-C10 da fixture da matriz ⇒ `classify_step` retorna cognitive com reason citando C10; E teste negativo global: NENHUM bloco emitido deterministicamente em NENHUM round-trip contém `:has-text(<literal>)` cujo literal case um `observed_value` do dicionário correspondente (varredura pós-emissão sobre a saída inteira); (CL) tabela de casos de `classify_step` cobrindo cada condição C1-C10 individualmente (um step que falha SÓ aquela condição ⇒ cognitive/omit com reason correspondente) usando a fixture da matriz + casos inline. NÃO modifique `code_generator.py` nem `step_validator.py` — se o round-trip revelar divergência emissor×validador, o bug é do EMISSOR nesta fase (os validadores são a spec). Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_deterministic_emitter.py` passa (round-trip verde contra golden v1, v2 rico E fixture da matriz; testes RT2, RT4 e tabela C1-C10 inclusos).
  - [x] `python aegis_sanitizer/test_sanitizer_execution_plan.py` e `python aegis_sanitizer/test_step_validator_hints.py` verdes SEM nenhuma edição nesses arquivos (se precisarem de edição, é regressão real — parar e reportar).
  - [x] `git status` mostra mudanças somente em `aegis_sanitizer/deterministic_emitter.py` e `aegis_sanitizer/test_deterministic_emitter.py`.

---

### [SUBAGENTE 05] - `_generate_scoped_blocks`: extração compartilhada (H3)
> ✅ CONCLUÍDO — `_generate_scoped_blocks(mode="correct"|"write")` extraída; `_surgical_correct_scoped` vira caller fino (equivalência testada). Ast-lint de bloco optional (Seção 7) incluído: `try` só `Expr`/`Call` de `runner.*`/`page.*`, `except` deve conter `print` referenciando a variável do erro. Modo `"write"` reservado sem caller ainda (integração é do SUBAGENTE 07).
- **🎯 Objetivo:** Generalizar o miolo de prompt/parse/splice de `_surgical_correct_scoped` numa função `_generate_scoped_blocks` parametrizável ("corrija este bloco" vs "escreva este bloco novo"), com `_surgical_correct_scoped` virando caller — refatoração mecânica, comportamento idêntico.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2.3 passo 5, 5.3, 7 — linha do ast-lint), `aegis_sanitizer/code_generator.py` (funções `_surgical_correct_scoped`, `_build_scoped_edit_plan` linhas 1163-1205, guard coluna-0 linha 1333, fallback escopado→full linhas 1501-1505)
  - Modificar: `aegis_sanitizer/code_generator.py`
  - Criar: `aegis_sanitizer/test_generate_scoped_blocks.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é uma refatoração mecânica em `aegis_sanitizer/code_generator.py`: extrair de `_surgical_correct_scoped` o miolo reutilizável — montagem do prompt-moldura, chamada ao gateway, parse do formato BEGIN_STEP/END_STEP (mesma regex atual), guard de rejeição para `def`/`class` em coluna-0 (reutilize VERBATIM o guard existente, linha ~1333) e splice por substituição de linhas — para uma nova função `_generate_scoped_blocks`, parametrizada pelo texto-moldura do prompt (ex.: parâmetro `mode` ou `frame_text`, distinguindo 'corrija este bloco existente' de 'escreva este bloco novo a partir do plano'). `_surgical_correct_scoped` vira um caller fino da função extraída, com comportamento EXTERNO byte-idêntico ao atual (mesmos retornos, mesmos fallbacks para None). Inclua na função extraída o ast-lint barato de blocos optional descrito na Seção 7 do plano (linha de mitigação do risco 'except engole erro'): quando um bloco respondido contém `try/except` de step optional, valide via `ast` que o `except` re-imprime o erro (chamada a `print` com a variável do except) e que o corpo do `try` só contém chamadas em `runner.*`/`page.*` (Expr de Call) — bloco que viole ⇒ retorno None (mesmo tratamento de resposta malformada). NÃO altere `_build_scoped_edit_plan`, `_surgical_correct`, o escopo por `lineno` nem o fallback escopado→full (`code_generator.py:1501-1505`) — tudo intacto. Nenhuma chamada nova a `_generate_scoped_blocks` além de `_surgical_correct_scoped` nesta tarefa (a integração da geração nova é de outra tarefa). Escreva `aegis_sanitizer/test_generate_scoped_blocks.py` (convenção `python <arquivo>.py` do módulo, gateway mockado) cobrindo: resposta bem-formada ⇒ splice correto; resposta malformada (sem par BEGIN/END) ⇒ None; resposta com `def` coluna-0 ⇒ None; bloco optional com except sem print ⇒ None; e um teste de equivalência confirmando que `_surgical_correct_scoped` continua retornando o mesmo resultado de antes para uma fixture de correção escopada simples. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_generate_scoped_blocks.py` passa.
  - [x] `python aegis_sanitizer/test_error_selector_config.py` e `python aegis_sanitizer/test_dryrun_multirow.py` verdes sem edição.
  - [x] `git status` mostra mudanças somente em `aegis_sanitizer/code_generator.py` + o teste novo.

---

### [SUBAGENTE 06] - Política anti-drift no Ralph: `_restore_deterministic_blocks` + ciclo de vida do manifest (H5)
> ✅ CONCLUÍDO — restore incondicional + fail-fast condição dupla (ordem nunca dispara, conteúdo dispara) + ciclo de vida do manifest, todos testados. Bug real achado e corrigido na implementação: `_parse_step_blocks` sem limite deixava o ÚLTIMO step engolir o `if __name__` (nenhuma âncora depois dele) — restore apagaria o `__main__` silenciosamente; corrigido com `_step_blocks_within_scenario` (AST-bounded a `execute_scenario_default`).
- **🎯 Objetivo:** Implementar o restore incondicional de blocos deterministic adulterados pela LLM no Ralph Loop, o fail-fast de bug de emissor com condição dupla, a atualização `cognitive_patched` pós-correção e o ciclo de vida do manifest (toda rota de geração sobrescreve; `plan_checksum` gate).
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2.4 e 5.2 INTEIRAS), `aegis_sanitizer/deterministic_emitter.py` (`emit_step_block`, formato do manifest), `aegis_sanitizer/code_generator.py` (`generate()` linhas 517-805, ponto pós-`_normalize_boilerplate` linha ~564, `_parse_step_blocks`, `live_error_step_ids` linhas 1468-1487, reordenação automática linhas 667-669)
  - Modificar: `aegis_sanitizer/code_generator.py`
  - Criar: `aegis_sanitizer/test_restore_deterministic_blocks.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é implementar em `aegis_sanitizer/code_generator.py` a política anti-drift da Seção 5.2 do plano `.specs/plano-codegen-hibrido-deterministico.md`. (1) Nova função `_restore_deterministic_blocks(bot_code, manifest, target_scope, plan, dicionario) -> (novo_codigo, restored: list)`: para cada step do manifest com `provenance == 'deterministic'` e `step_id ∉ target_scope`, localize o bloco atual via `_parse_step_blocks`; bloco AUSENTE (âncora removida em rewrite full-file) ⇒ `continue` — NÃO é caso de restore nem de erro (vira `MISSING_STEPS` no fluxo normal); bloco presente ⇒ regenere o canônico via `emit_step_block(step, dicionario)` (importe de `aegis_sanitizer.deterministic_emitter`; regenerado, nunca armazenado) e, se o texto atual difere, re-splice o canônico no lugar e adicione o step_id a `restored`. O restore é INCONDICIONAL (sem comparação de sha1 — `emit_step_block` é determinístico e re-splicear é idempotente; `block_sha1` do manifest é telemetria, nenhuma decisão depende dele). `target_scope` = `target_step_ids` da correção corrente + `live_error_step_ids` + `after_step_id` de TODO `required_reopen` pendente (achado M1: o re-disparo exigido por `validate_required_reopen_patterns` vive textualmente no bloco do `after_step_id` — um restore que não o poupasse causaria MISSING_REOPEN_PATTERN ⇒ oscilação). (2) UM ponto de chamada em `generate()`: logo após `_normalize_boilerplate` (linha ~564), DENTRO do loop de tentativas — um ponto único cobre todas as tentativas (achado M3: NÃO são 2 pontos). Guardas de no-op: manifest ausente ⇒ no-op; `steps` vazio ⇒ no-op por construção; `plan_checksum` do manifest ≠ sha1 do `plano_execucao.json` atual ⇒ no-op (cobre re-sanitização que renumera step_ids). (3) FAIL-FAST restrito — `RuntimeError('bug no deterministic_emitter para st_XXX — não gaste tentativas de LLM')` dispara SOMENTE quando as DUAS condições valem: (a) o erro de validação aponta (via `step_id` ou `lineno` mapeado a bloco) para um step_id presente em `restored` NESTA MESMA tentativa; E (b) o tipo do erro é de CONTEÚDO do bloco — padrões de resiliência (`MISSING_*`, `INVALID_CHAINED_LOCATOR_SHAPE`, `WEAK_SELECTOR_WITHOUT_ANCHOR`, ...) ou contrato de chamada. Erros de ORDEM/CONTAGEM (`STEP_ID_MISMATCH`, `COUNT_MISMATCH`, `MISSING_STEPS`, `EXTRA_STEPS`) NUNCA disparam o fail-fast (achado I6: a LLM pode mover um bloco deterministic num rewrite full-file; o restore o reverte NO LUGAR MOVIDO e o erro de ordem aponta para um bloco sem culpa; além disso a reordenação automática, `code_generator.py:667-669`, só roda quando `error_types ⊆ {STEP_ID_MISMATCH}`). Erros de dry-run NUNCA disparam o fail-fast nesta v1 (achado R3: erros de `dry_run_bot` não carregam `lineno` — `step_validator.py:1750-1758`, só `type`/`exception_type`/`detail` — impossível atribuí-los a um bloco). (4) Correção legítima mirando bloco deterministic: o step entra em `target_scope`, o restore o ignora, e ao final da TENTATIVA VENCEDORA o manifest atualiza `provenance: 'cognitive_patched'` (com `reason` citando a correção) e é regravado em disco. (5) Ciclo de vida do manifest (Seção 2.4): TODA rota de geração bem-sucedida termina gravando `code/generation_manifest.json` ao lado de `bot_producao.py` — a rota híbrida grava o manifest real; a rota full-LLM (flag off, skills, plano ausente, fallback) grava `{'generator_version': 'full-llm', 'steps': {}}` POR CIMA do que existir (previne manifest stale armando o restore contra bot não-híbrido). Implemente a gravação como helper (`_write_generation_manifest`) chamado nos pontos de sucesso de `generate()` que já existem hoje (rota full-LLM atual) — a rota híbrida em si chega em outra tarefa e reutilizará o helper. Regra de `lineno` (working agreement nº 5): qualquer erro novo que esta tarefa reporte carrega `step_id` ou `lineno`. Escreva `aegis_sanitizer/test_restore_deterministic_blocks.py` (convenção `python <arquivo>.py`, mocks) cobrindo: bloco adulterado fora do escopo ⇒ restaurado; dentro do escopo ⇒ preservado; bloco AUSENTE ⇒ ignorado sem erro; `after_step_id` de `required_reopen` pendente ⇒ poupado; manifest stale (`plan_checksum` divergente) ⇒ no-op; sem manifest ⇒ no-op; erro de ORDEM apontando bloco restaurado ⇒ NÃO dispara fail-fast; erro de CONTEÚDO em bloco restaurado na mesma tentativa ⇒ dispara `RuntimeError`; rota full-LLM grava manifest `{'generator_version': 'full-llm', 'steps': {}}`. NÃO altere `_surgical_correct`, `_surgical_correct_scoped`, `_build_scoped_edit_plan` nem `step_validator.py`. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_restore_deterministic_blocks.py` passa (todos os 9 cenários listados).
  - [x] `python aegis_sanitizer/test_error_selector_config.py`, `python aegis_sanitizer/test_dryrun_multirow.py` e `python aegis_sanitizer/test_generate_scoped_blocks.py` verdes sem edição.
  - [x] `git status` mostra mudanças somente em `aegis_sanitizer/code_generator.py` + o teste novo.

---

### [SUBAGENTE 07] - Integração híbrida em `_generate_new_code` atrás de `AEGIS_CODEGEN_HYBRID` (H4)
> ✅ CONCLUÍDO — `_generate_new_code_hybrid` + `_render_hybrid_slots_context` ligados atrás da flag (default `false`); zero slots ⇒ zero chamadas LLM; manifest híbrido reconciliado com o ciclo de vida existente do restore/finalize (senão seria descartado). T1-T5 nomeados e verdes.
- **🎯 Objetivo:** Ligar o motor: fluxo da Seção 2.3 dentro de `_generate_new_code`, atrás da flag `AEGIS_CODEGEN_HYBRID` (default `false`), com curto-circuito full-LLM (flag off / skills / plano ausente), chamada única de slots via `_generate_scoped_blocks`, convenção de bloco-vazio para optional e fallback full-LLM por slot faltante.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2.3 INTEIRA, 3.2, 2.4), `aegis_sanitizer/deterministic_emitter.py` (`classify_step`, `build_skeleton`), `aegis_sanitizer/code_generator.py` (`_generate_new_code` linha 807, `_render_plan_for_prompt` linha 205, `_generate_scoped_blocks` de [SUBAGENTE 05], `_write_generation_manifest` de [SUBAGENTE 06], renderização de correções em `_surgical_correct` linhas 1360-1418)
  - Modificar: `aegis_sanitizer/code_generator.py`
  - Criar: `aegis_sanitizer/test_hybrid_generation.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é integrar a geração híbrida em `_generate_new_code` (`aegis_sanitizer/code_generator.py:807`), seguindo a Seção 2.3 do plano `.specs/plano-codegen-hibrido-deterministico.md`. FLUXO: (1) plano + dicionário carregados como hoje. (2) Se `AEGIS_CODEGEN_HYBRID` != 'true' (default `false` nesta tarefa) OU `skills_used` não-vazio OU plano ausente ⇒ fluxo full-LLM ATUAL, byte-idêntico (rota preservada, não deletada) + gravação do manifest full-LLM (`_write_generation_manifest` com `{'generator_version': 'full-llm', 'steps': {}}`). (3) Senão: `decisions = {step_id: classify_step(...)}` com log de 1 linha por step (`[HYBRID] st_001 -> deterministic (click simples)`), respeitando `AEGIS_CODEGEN_FORCE_LLM_STEPS` (CSV de step_ids rebaixados a cognitive). (4) `skeleton, manifest = build_skeleton(plan, dicionario, pending_corrections)` — blocos deterministic prontos; slots cognitivos como placeholder `# [PASSO N] <descrição>` + `# AEGIS_COGNITIVE_SLOT step_id=\"st_XXX\" motivo=\"<reason>\"` + `pass` (já implementado no emitter); steps skip omitidos. (5) Se houver slots cognitivos: UMA chamada LLM via `_generate_scoped_blocks` (modo 'escreva este bloco novo') pedindo SOMENTE esses blocos no formato BEGIN_STEP/END_STEP. O prompt dos slots contém, NESTA enumeração (fonte canônica = Seção 2.3 passo 5 do plano): playbook (seções relevantes) + fatia do plano dos slots via `_render_plan_for_prompt` (já existente, linha 205) + blocos vizinhos já emitidos como contexto SOMENTE-LEITURA + dicionário + regras cognitivas (Padrão Q/N, contrato optional) + as entradas de `pending_corrections` cujo `step_id` OU `required_reopen.after_step_id` esteja entre os slots, na MESMA renderização de `_surgical_correct` (`code_generator.py:1360-1418` — qa_insight, tentativas fracassadas, correção requisitada; achado I3: sem isso a C8 é autossabotada — o step vai pra LLM POR CAUSA da correção e a LLM não a veria). Inclua no prompt o CONTRATO OPTIONAL com o template canônico da Seção 3.2 (try/except com print do erro) E a convenção de bloco-vazio: a LLM que decidir NÃO emitir um optional retorna `# BEGIN_STEP st_XXX` + `# [PASSO N] <descrição>` + `# AEGIS_COGNITIVE_SLOT step_id=\"st_XXX\" motivo=\"optional não emitido: <justificativa curta>\"` + `# END_STEP st_XXX` — o splice ACEITA (não é slot faltando) e o manifest registra `provenance: 'cognitive', reason: 'optional_omitted'`; o comentário remanescente mantém `step_id=\"...\"` parseável. (6) Resposta sem par BEGIN/END nem bloco-vazio para algum slot, OU splice inválido ⇒ fallback full-LLM na MESMA tentativa (mesma semântica de `code_generator.py:1501-1505`), gravando manifest full-LLM. (7) Sucesso híbrido ⇒ grava o manifest real via `_write_generation_manifest` e o arquivo completo entra no pipeline EXISTENTE sem mudança (`_validate_syntax` → `_normalize_boilerplate` → validadores → dry_run → Ralph). Zero slots ⇒ ZERO chamadas LLM na geração. Documente em comentário que `gateway.is_active()` continua exigido mesmo com zero slots (achado M2 — custo é zero chamadas, não zero configuração; relaxar fica para depois). TESTES (`aegis_sanitizer/test_hybrid_generation.py`, convenção `python <arquivo>.py`, gateway SEMPRE mockado): (T1-descartável) com flag `false`, o prompt montado para o gateway é IDÊNTICO ao atual via snapshot — marque o teste com comentário `# GATE DESCARTÁVEL: remover após flip do default (H8.5) — codifica detalhe de implementação, não contrato`; (T2) flag `true` + fixture SEM slot cognitivo ⇒ bot gerado passa todos os validadores + dry run com ZERO chamadas ao gateway (mock assertando zero calls) e manifest `hybrid-1` gravado; (T3 — achado I7, obrigatório) flag `true` + fixture COM slot cognitivo ⇒ mock do gateway retornando BEGIN_STEP/END_STEP válido ⇒ asserte que a resposta foi SPLICEADA no skeleton (bloco presente no arquivo final) E que NÃO houve fallback full-LLM (UMA única chamada ao gateway, com o prompt de slots — não o prompt de arquivo inteiro); (T4) fixture com step `optional` onde o mock responde o bloco-vazio da convenção ⇒ splice aceita, manifest registra `optional_omitted`, validadores PASS; (T5) mock respondendo sem um dos slots ⇒ fallback full-LLM na mesma tentativa. Use os planos golden (`.specs/golden/real_portal_segura_001_v2/`, `.specs/golden/synthetic_hybrid_matrix/`) como fixtures. NÃO altere `step_validator.py`, `deterministic_emitter.py`, `sanitizer.py` nem nada em `aegis_runner/`/`aegis_cockpit/`. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_hybrid_generation.py` passa (T1-T5).
  - [x] `python aegis_sanitizer/test_deterministic_emitter.py`, `python aegis_sanitizer/test_generate_scoped_blocks.py`, `python aegis_sanitizer/test_restore_deterministic_blocks.py`, `python aegis_sanitizer/test_error_selector_config.py`, `python aegis_sanitizer/test_dryrun_multirow.py` verdes sem edição.
  - [x] `git status` mostra mudanças somente em `aegis_sanitizer/code_generator.py` + o teste novo.

---

### [SUBAGENTE 08] - Rota determinística de reintrodução de `sup_` (`reintroduce_step_id`) (H6)
> ✅ CONCLUÍDO — `_apply_deterministic_sup_reintroductions` com dois pontos de integração (pós-skeleton híbrido + pré-LLM em `_surgical_correct`), uma implementação só. E2e confirmado: `sup_003` reintroduzido com wrapper optional, posição relativa correta, `validate_bot_against_plan` PASS, zero chamadas LLM (prova mecânica do contrato D6, teste 4.1(3) do plano).
- **🎯 Objetivo:** Implementar a rota barata de reintrodução: uma correção em `correcoes_acumuladas.json` com `reintroduce_step_id: "sup_NNN"` faz o gerador inserir deterministicamente o bloco do `sup_` (emitido + wrapper optional) na posição relativa correta do plano, sem LLM.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seção 3.1), `aegis_sanitizer/deterministic_emitter.py` (`emit_step_block`, `_emit_optional_wrapper`), `aegis_sanitizer/code_generator.py` (leitura de `pending_corrections`, fluxo de marcação `applied`), `aegis_sanitizer/step_validator.py` (`validate_bot_against_plan` — tolerância de subsequência e `planned_set_for_reopen`)
  - Modificar: `aegis_sanitizer/code_generator.py`
  - Criar: `aegis_sanitizer/test_reintroduce_sup_step.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é implementar em `aegis_sanitizer/code_generator.py` a rota determinística de reintrodução de steps suprimidos (Seção 3.1 do plano `.specs/plano-codegen-hibrido-deterministico.md`). Comportamento: quando uma entrada de `pending_corrections` (lida de `correcoes_acumuladas.json`, fluxo existente) carrega o campo novo `reintroduce_step_id: 'sup_NNN'`, o gerador: (1) localiza o step `sup_NNN` no plano (`plano_execucao.json`); (2) gera o bloco via `emit_step_block` do `deterministic_emitter` SEMPRE envelopado em `_emit_optional_wrapper` (um `sup_` reintroduzido é por definição não-fatal — template da Seção 3.2, except imprime o erro); (3) insere o bloco na posição relativa correta segundo a ordem do array `steps` do plano (entre os blocos dos steps emitidos vizinhos — use as âncoras `# [PASSO N]`/step_id existentes no código para achar o ponto de inserção); (4) o fluxo existente de marcação `applied` da correção segue inalterado. A inserção é determinística — NENHUMA chamada LLM. Funciona tanto na geração nova híbrida quanto no ciclo cirúrgico (escolha o ponto de integração que cubra os dois com UMA implementação — ex.: pós-skeleton na geração nova e como passo determinístico pré-LLM no ciclo de correção — e documente a escolha em comentário). O validador já aceita o `sup_` emitido (subsequência hint-aware + `planned_set_for_reopen` incluem ids skip — NÃO toque em `step_validator.py`). Se `reintroduce_step_id` apontar para um id inexistente no plano, logue warning e trate a correção pelo fluxo LLM normal (nunca crashe). Escreva `aegis_sanitizer/test_reintroduce_sup_step.py` (convenção `python <arquivo>.py`, gateway mockado) cobrindo: correção com `reintroduce_step_id: 'sup_003'` ⇒ bot final contém o bloco do `sup_003` com wrapper try/except na posição relativa correta (entre os vizinhos certos do plano) E `validate_bot_against_plan` retorna PASS (este é o teste 4.1(3) do plano — prova mecânica do contrato D6); id inexistente ⇒ warning + fluxo normal; manifest (quando híbrido) registra o step reintroduzido com `provenance: 'deterministic'` e reason citando a reintrodução. NÃO altere `aegis_cockpit/` (o campo funciona via JSON; UI é fora de escopo). Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_reintroduce_sup_step.py` passa.
  - [x] `python aegis_sanitizer/test_hybrid_generation.py` e `python aegis_sanitizer/test_restore_deterministic_blocks.py` verdes sem edição.
  - [x] `git status` mostra mudanças somente em `aegis_sanitizer/code_generator.py` + o teste novo.

---

### [SUBAGENTE 09] - Playbook + contrato de prompt do reflection (H7)
> ✅ CONCLUÍDO — Seção 4 nova no playbook + parágrafo de restore no reflection prompt. Efeito colateral honesto: playbook embutido verbatim no prompt full-LLM introduziu a string `AEGIS_COGNITIVE_SLOT` nele, quebrando 1 assert textual do teste T1 (já marcado "gate descartável" desde H4) — removida, mantidos os 2 sentinelas reais (`gateway.calls==1`, `_hybrid_manifest is None`); confirmado no diff.
- **🎯 Objetivo:** Documentar no playbook a divisão híbrida ("o que a LLM escreve e o que ela nunca escreve") e ajustar o texto do reflection prompt para citar a política de restore — mudanças de TEXTO de prompt/playbook, zero lógica nova.
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2.3, 3.2, 3.3, 5.2), `aegis_sanitizer/code_generator.py` (`_surgical_correct_with_reflection` — texto do prompt; prompt de slots de [SUBAGENTE 07])
  - Modificar: `aegis_mentor/skills/rpa-copilot-coder.md`, `aegis_sanitizer/code_generator.py` (SOMENTE strings de prompt do reflection)
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa tem duas partes de documentação/prompt, zero lógica nova. PARTE 1 — `aegis_mentor/skills/rpa-copilot-coder.md`: adicione uma seção nova 'Geração híbrida — o que a LLM escreve e o que ela nunca escreve' cobrindo: (a) na geração híbrida, blocos deterministic são emitidos por motor e a LLM recebe SOMENTE slots cognitivos marcados `# AEGIS_COGNITIVE_SLOT step_id=\"...\"` — ela responde no formato BEGIN_STEP/END_STEP apenas para esses slots; (b) os casos que permanecem cognitivos: Padrão Q dinâmico (`has_text_original` ≠ `has_text` — decidir entre literal sanitizado e f-string com `row`, este é um dos poucos pontos onde a LLM ainda escreve código, Seção 3.3 do plano), decisão de emitir step `optional`, Padrão N (menu `>>`), seletor com valor de negócio (parametrizar com `row.get(...)`, nunca literal gravado), steps alvo de correção pendente; (c) o template canônico de step optional (try/except não-fatal com print do erro no except — copie o template exato da Seção 3.2 do plano) e a convenção de bloco-vazio para optional NÃO emitido (par BEGIN/END contendo só comentários com `step_id=\"...\"` e motivo); (d) PROIBIÇÃO: no modo reflection/correção, nunca tocar blocos deterministic fora do escopo da correção — eles serão restaurados automaticamente pela política anti-drift. PARTE 2 — em `aegis_sanitizer/code_generator.py`, localize o texto do prompt de `_surgical_correct_with_reflection` e adicione um parágrafo curto: 'Blocos marcados como deterministic no manifest e fora do escopo desta correção serão RESTAURADOS automaticamente à forma canônica após sua resposta — não gaste tokens reescrevendo-os; qualquer alteração neles será descartada.' NÃO altere nenhuma lógica Python (só literais de string de prompt) e NÃO toque no prompt de slots da geração nova (já contém o contrato, escrito em outra tarefa — apenas confira consistência de terminologia e reporte divergências em vez de editar). Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] Toda a suíte de prompts continua verde: `python aegis_sanitizer/test_hybrid_generation.py` (exceto se o snapshot T1-descartável cobrir o reflection — nesse caso atualizar SÓ o snapshot, documentando), `python aegis_sanitizer/test_error_selector_config.py`, `python aegis_sanitizer/test_dryrun_multirow.py`.
  - [x] Snapshot manual: renderizar o reflection prompt com fixture híbrida (script ad-hoc) e confirmar visualmente o parágrafo da política de restore; leitura humana da seção nova do playbook.
  - [x] `git status` mostra mudanças somente em `aegis_mentor/skills/rpa-copilot-coder.md` e `aegis_sanitizer/code_generator.py`.

---

### [SUBAGENTE 10] - Validação de ponta a ponta real + regression gate + flip do default (H8 — working agreement nº 1)
> ✅ CONCLUÍDO (retry8, Claude Opus 4.8, 2026-07-13) — gate fechado com evidência verificada por mim de forma independente (49 arquivos, 768K, `scratchpad/evidence_h8_retry8/`; conferi eu mesmo `find`/listagem + abri 3 arquivos-chave: `st_062` dinâmico, `VERDICT.md`=APROVADO, log de `st_025 HEALED`). Os 6 passos passaram: (1) metodologia corrigida (bot antigo apagado antes de injetar correção); (2) regeneração limpa 51 det/12 cog, manifest `hybrid-1`, Padrão Q dinâmico, C10 parametrizado; (3) 3 execuções reais, `st_025` HEALED nas 3, avança até `st_038` (mesmo blocker do baseline, drift de data); (4) `aegis-regression-gate` APROVADO explícito; (5) ciclo cirúrgico real com escopo exato (det `st_010` + cog `st_023`), manifest atualizado; (6) 12 suítes verdes. `AEGIS_CODEGEN_HYBRID=true` (default desde retry7, confirmado intacto, não repetido). Nenhum commit feito. Caveat novo achado e documentado na Seção 8 do plano (`Fora de escopo`): `EXTRA_STEPS` hard-fail quando ciclo cirúrgico roda com `required_reopen` já `applied` — pré-existente, fora do escopo deste backlog.
>
> ~~❌ FALHOU (retries 1-7)~~ — Passo 1 (comparação de regeneração) reprova, 2/2 rodadas independentes reproduzem o mesmo defeito. **Classe: PLANO** (gap na Seção 3.3, não erro de execução do subagente — achados positivos do próprio passo 1 confirmam a implementação: manifest correto 51 det./12 cog., step_ids/métodos idênticos ao baseline, C10 funcionando e até corrigindo um hardcode preexistente em st_023/024/025). Passos 2-5 NÃO executados (regra de parar no 1º reprovado). Nenhum flip, nenhum commit.
>
> **Defeito:** `st_062` (Padrão Q, C3 ⇒ cognitive corretamente) — o slot cognitivo gerado pelo LLM produziu `parent={"selector": ".mat-row", "has_text": "daniel setttt 22401666818 FIPE"}` (literal gravado, só sem o prefixo `PRO-80935` já removido pelo Sanitizer) em vez de `parent={"selector": ".mat-row", "has_text": f"{row.get('nome_cliente', '')} {row.get('cpf_cliente', '')} FIPE"}` (composição dinâmica, forma do baseline) — violação direta da regra ANTI-HARDCODE do Padrão Q documentada no próprio playbook (`aegis_mentor/skills/rpa-copilot-coder.md` ~linha 261, que usa ESTE MESMO caso como exemplo do bug a evitar). Causa raiz confirmada em `step_validator.py:983-987`: `MISSING_PARENT_HAS_TEXT` aceita `code_has_text == plan_has_text` (match literal) como igualmente válido a uma composição dinâmica — nenhum validador força reconstrução via `row.get(...)` quando `sanitization_notes` indica correção Padrão Q. O LLM, vendo o erro do validador citando o literal, copia o literal (caminho mais barato) em vez de reconstruir — e passa em todos os validadores e no dry run. Com dataset de 1 linha (fixture de teste) não quebra; com 2+ linhas de clientes diferentes, sempre mira a mesma pessoa/CPF gravado — exatamente a classe de bug que C10 foi desenhado para C10, só que na forma C3/Padrão Q.
>
> **Evidência preservada:** `scratchpad/evidence_subagente10/` (bots baseline/run1/run2, manifests, diff completo, entrada do plano de `st_062`) — verificada por mim diretamente (`diff_baseline_vs_hybrid.txt` linhas 618-630).
>
> **Recomendação (não aplicada — decisão de design, não de execução):** (a) fortalecer `_render_hybrid_slots_context`/prompt do slot para instruir EXPLICITAMENTE reconstrução via `row.get(...)` quando o step tem `sanitization_notes` com `padrao_q`, e/ou (b) endurecer `MISSING_PARENT_HAS_TEXT` em `step_validator.py` para exigir `"has_text" in code_parent_dynamic_keys` (rejeitar match literal) especificamente quando `sanitization_notes` indica correção Padrão Q. Volta para `plan-critic`/emenda no plano antes de nova tentativa de H8 — não é redespacho cego do mesmo bloco.
- **🎯 Objetivo:** Provar contra site real e contra o baseline que o híbrido não regride: regeneração comparada, execução real com autocomplete, `aegis-regression-gate`, ciclo cirúrgico real — e, SÓ com tudo APROVADO, flip do default `AEGIS_CODEGEN_HYBRID=true` em commit isolado.
- **📂 Escopo de Arquivos:**
  - Ler: TODOS os arquivos modificados por [SUBAGENTE 03]-[SUBAGENTE 09]; `.specs/baseline-codegen-hibrido.md`; `projects/portal_segura/tests/001_teste/*` (NUNCA modificar o original)
  - Modificar: `aegis_sanitizer/code_generator.py` (SOMENTE o default da flag, e SOMENTE no passo 5) + artefatos de execução em CÓPIA de `projects/portal_segura/tests/001_teste/`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é o gate final da demanda 'code generator híbrido' (H8 do plano `.specs/plano-codegen-hibrido-deterministico.md`) — validação REAL, não mockada (working agreement nº 1 do CLAUDE.md: mudança em selector/DOM/timing não está pronta até rodar contra navegador real). Execute NA ORDEM, parando e reportando no primeiro REPROVADO: (1) COMPARAÇÃO DE REGENERAÇÃO: copie `projects/portal_segura/tests/001_teste/` inteiro para uma pasta de trabalho descartável (nunca toque no original); com `AEGIS_CODEGEN_HYBRID=true` (PowerShell: `$env:AEGIS_CODEGEN_HYBRID='true'`), regenere a Fase 4 (`python aegis_sanitizer/code_generator.py --project-dir <cópia>`) e compare o bot gerado com o baseline congelado em `.specs/baseline-codegen-hibrido.md` (bot de referência `projects/portal_segura/tests/001_teste/code/bot_producao.py`): mesmos step_ids na mesma ordem, mesmos métodos runner por step, diffs SÓ onde a classificação mudou a forma — registre o diff comentado; confirme que `code/generation_manifest.json` foi gravado com `generator_version: 'hybrid-1'` e conte deterministic × cognitive. (2) EXECUÇÃO REAL: rode o bot híbrido gerado contra o site real (invoque a skill `aegis-live-pilot` se for site novo; para o Portal Segura de referência, execução direta `python <cópia>/code/bot_producao.py` com o `.env` do projeto) — taxa de sucesso deve ser ≥ à do baseline registrado. OBRIGATÓRIO incluir cenário com autocomplete real na forma REAL do padrão (fill que precede CLICK em opção de painel dinâmico — ex. st_022→st_023 do 001_teste): valida a C9 recalibrada (HUMAN_LIKE vs DIRECT é dependente do site, nenhum validador estático cobre) E a C10 (o click da opção deve sair parametrizado com `row.get(...)`, NUNCA com o literal gravado — inspecione o código gerado desse step e registre a evidência). (3) REGRESSION GATE: invoque a skill `aegis-regression-gate` (mudou core `aegis_sanitizer/`) — bot de referência default, N runs, comparação com baseline em `.specs/plans/*.baseline-*.md`; exige veredito APROVADO explícito. (4) CICLO CIRÚRGICO REAL: na cópia, introduza uma correção QA mirando um bloco deterministic e outra mirando um cognitivo (via `correcoes_acumuladas.json`, fluxo normal); rode o ciclo de correção e confirme: modo escopado ativo nos dois casos (logs — NÃO caiu em full rewrite), manifest atualizado (`cognitive_patched` no bloco deterministic corrigido), blocos deterministic fora do escopo intactos. (5) FLIP DO DEFAULT — SOMENTE se (1)-(4) APROVADOS: mude o default de `AEGIS_CODEGEN_HYBRID` para `true` em `aegis_sanitizer/code_generator.py`, rode a suíte completa (`python aegis_sanitizer/test_deterministic_emitter.py && python aegis_sanitizer/test_hybrid_generation.py && python aegis_sanitizer/test_generate_scoped_blocks.py && python aegis_sanitizer/test_restore_deterministic_blocks.py && python aegis_sanitizer/test_reintroduce_sup_step.py && python aegis_sanitizer/test_sanitizer_execution_plan.py && python aegis_sanitizer/test_step_validator_hints.py && python aegis_sanitizer/test_error_selector_config.py && python aegis_sanitizer/test_dryrun_multirow.py && python aegis_sanitizer/test_weak_selector_enforcement.py && python aegis_runner/test_runner_integration.py` — no PowerShell rode um por um checando `$?`), remova o teste-snapshot T1-descartável de `test_hybrid_generation.py` (gate marcado como descartável pós-flip pelo próprio plano) e proponha o flip como COMMIT ISOLADO (revert trivial) — não misture com nenhuma outra mudança. Apague as pastas de trabalho ao final. Reporte cada passo com evidência bruta (saídas de comando, taxas, diffs). Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] Passo 1: regeneração híbrida comparada ao baseline — mesmos step_ids/métodos, diff registrado e justificado, manifest `hybrid-1` gravado.
  - [ ] Passo 2: execução real com taxa ≥ baseline, incluindo cenário de autocomplete real; evidência de que o click de opção saiu parametrizado com `row.get(...)` (C10) — nunca literal.
  - [ ] Passo 3: `aegis-regression-gate` com veredito APROVADO explícito.
  - [ ] Passo 4: modo escopado confirmado nos dois casos (deterministic e cognitivo) + manifest `cognitive_patched`.
  - [ ] Passo 5: suíte completa verde com default `true`, snapshot descartável removido, flip em commit isolado proposto.

---

### [SUBAGENTE 12] - Correção Padrão Q: prompt prescritivo (Q-a) + validador endurecido (Q-b) + ajuste de teste (remediação pós-reprovação de [SUBAGENTE 10]/H8)
> ✅ CONCLUÍDO (implementado por Claude Fable 5) — Q-a + Q-b + os 3 ajustes coordenados do round-trip, todos aplicados. Verifiquei de forma independente (não só o self-report): rodei `validate_resilience_patterns` direto contra `st_062` do golden v2 com bot fabricado hardcoded ⇒ `HARDCODED_PARENT_HAS_TEXT` dispara, `detail` nomeia `nome_cliente`('daniel setttt')+`cpf_cliente`('22401666818') e prescreve a f-string exata. 56 testes no round-trip, 9 suítes verdes, blast radius exatamente nos 3 arquivos do escopo. Nota honesta do subagente: auto-corrigiu um `continue` próprio que pularia o check de `coords` no meio da implementação — confirmado no diff que o bloco antigo ficou intacto em `else:`.
>
> **Próximo passo:** redespachar [SUBAGENTE 10]/H8 integralmente (Fase 9c) — reprovação foi no Passo 1, nunca rodou os passos 2-5.
- **🎯 Objetivo:** Fechar o gap achado no gate H8 — o slot cognitivo de Padrão Q gerava `parent.has_text` hardcoded (literal gravado) em vez de composição dinâmica com `row.get(...)`. Implementar as DUAS correções decididas em `plan-critic` (rodadas 3+4, ambas registradas no cabeçalho e na Seção 3.3 emendada de `.specs/plano-codegen-hibrido-deterministico.md`): prompt prescritivo (Q-a) + novo erro de validador `HARDCODED_PARENT_HAS_TEXT` (Q-b) + os 3 ajustes coordenados no round-trip que a rodada 4 identificou como faltantes (não é "1 ajuste de fixture" — ver DoD).
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seção 3.3 INTEIRA — está emendada duas vezes, rodadas 3 e 4, leia as duas emendas), `aegis_sanitizer/step_validator.py` (`validate_resilience_patterns` linha ~774, especialmente o bloco `parent`/`MISSING_PARENT_HAS_TEXT` linhas 967-1001, e `_collect_observed_values`-equivalente se existir), `aegis_sanitizer/deterministic_emitter.py` (`_collect_observed_values` linha ~452, `_extract_has_text_literals` — mecânica de C10 a REIMPLEMENTAR, não importar, para não inverter a dependência conceitual validador→emissor), `aegis_sanitizer/code_generator.py` (`_render_hybrid_slots_context` linhas ~1393-1405 — a contradição interna do prompt), `aegis_sanitizer/test_deterministic_emitter.py` (`_force_classify_where_possible` linhas 631-676, `_KNOWN_COGNITIVE_ONLY_STEP_IDS` linha ~620, comentário linhas 605-614, `GOLDEN_CASES` linhas 687-692), `scratchpad/evidence_subagente10/st_062_plan_entry.txt` se ainda existir (evidência do achado original — pode ter sido limpo; se não existir, use a Seção 3.3 do plano que já documenta o caso completo)
  - Modificar: `aegis_sanitizer/step_validator.py`, `aegis_sanitizer/code_generator.py`, `aegis_sanitizer/test_deterministic_emitter.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é implementar a correção Q-a+Q-b do Padrão Q, especificada na Seção 3.3 (emendada rodadas 3 e 4) de `.specs/plano-codegen-hibrido-deterministico.md` — LEIA A SEÇÃO INTEIRA antes de codificar, ela tem toda a evidência de linha e a justificativa de cada decisão. Resumo executável:
  >
  > **Q-a — `aegis_sanitizer/code_generator.py`, `_render_hybrid_slots_context` (~linhas 1393-1405):** hoje o prompt diz algo como 'decida entre usar o literal sanitizado OU compor has_text dinamicamente com row.get(...) — julgamento seu' e, três parágrafos depois, tem uma proibição absoluta de hardcode que contradiz essa permissão. Substitua a frase permissiva por uma regra PRESCRITIVA: quando o step tiver `parent.has_text_original` (diferente de `parent.has_text`) E o literal residual de `has_text` contiver um ou mais `observed_value` do `dicionario.json`, a composição DEVE ser dinâmica — f-string com `row.get(\"<chave>\", \"\")` para cada chave cujo `observed_value` aparece no literal, preservando o texto estático residual (ex.: `\"FIPE\"`) como literal dentro da mesma f-string. Só é aceitável literal puro quando NENHUM `observed_value` casa (residual 100% estático). Elimine a contradição com a proibição de hardcode do mesmo prompt.
  >
  > **Q-b — `aegis_sanitizer/step_validator.py`, dentro de `validate_resilience_patterns`, no bloco que hoje só produz `MISSING_PARENT_HAS_TEXT` (~linhas 967-1001):** adicione um check NOVO e SEPARADO (tipo de erro NOVO `HARDCODED_PARENT_HAS_TEXT` — NÃO reutilize `MISSING_PARENT_HAS_TEXT`, é um modo de falha diferente e a mensagem é OPOSTA). Gatilho: step com `parent.has_text_original` presente E o `plan_has_text` (valor de `parent.has_text` do PLANO) contendo pelo menos um `observed_value` do `dicionario.json` como SUBSTRING (não igualdade — `parent.has_text` é sempre um valor COMPOSTO tipo 'nome cpf FIPE', nunca um `observed_value` inteiro; reimplemente a coleta de `observed_value` — é ~6 linhas, veja `_collect_observed_values` em `deterministic_emitter.py` como referência de forma, mas não importe de lá). Quando o gatilho dispara: `code_has_text == plan_has_text` (match literal) e `plan_has_text in code_selector` (literal embutido no seletor) DEIXAM de contar como prova válida — só `\"has_text\" in code_parent_dynamic_keys` (dinâmico) passa. Fora do gatilho (sem `has_text_original`, ou residual sem nenhum `observed_value`), o comportamento ATUAL fica intacto — não toque nesses casos. O `detail` do erro novo DEVE: (a) nomear as chaves candidatas derivadas (as chaves do dicionário cujos `observed_value` casaram por substring no literal), (b) prescrever a forma f-string com `row.get(...)`, (c) DEGRADAR para uma mensagem genérica de proibição de hardcode (sem nomear chave) se o match for vazio ou ambíguo (mais de uma chave com o MESMO `observed_value` — ex.: `'2026'` pode ser `ano_modelo` E `ano_fabricacao` no dicionário de referência; não adivinhe qual). O erro carrega `step_id` (mesmo padrão dos demais checks deste loop — veja como `MISSING_PARENT_HAS_TEXT` já faz isso). Considere exigir um comprimento mínimo pro substring (ex.: >=3 chars) para reduzir falso-positivo com `observed_value` curtos tipo `'Sim'`.
  >
  > **Ajuste do round-trip em `aegis_sanitizer/test_deterministic_emitter.py` (3 partes coordenadas — a Seção 3.3/rodada 4 do plano prova que fazer só uma delas deixa o round-trip VERMELHO):**
  > 1. Adicione um ramo a `_force_classify_where_possible` (linhas ~631-676): ANTES do ramo final `return EmissionDecision(\"deterministic\", ...)`, detecte a mesma condição do gatilho Q-b (`parent.has_text_original` presente + `observed_value` por substring no residual) e retorne `cognitive` nesse caso — é uma 4ª categoria de \"genuinamente impossível forçar\", ao lado de C1/C4/C5/C10.
  > 2. Adicione `st_062` a `_KNOWN_COGNITIVE_ONLY_STEP_IDS[\"real_portal_segura_001_v2\"]` (linha ~620) — sozinho isso é insuficiente (só rebaixa o `execution_hint` da cópia do plano), mas em conjunto com o item 1 acima, `st_062` vira placeholder cognitivo de verdade (fora de `code_ids`, como já ocorre com os steps de C10).
  > 3. Atualize o comentário longo nas linhas ~605-614 que enumera as impossibilidades genuínas forçadas (hoje cita só C1/C4/C5/C10) para incluir a 4ª categoria (valor de negócio em `parent.has_text` sob Padrão Q, a mesma detecção de Q-b).
  >
  > Depois de implementar, ADICIONE a célula obrigatória da matriz do round-trip (Seção 4.1 do plano): teste que uma fixture 'Padrão Q com valor de negócio no residual' (na forma do `st_062` — `parent.has_text_original` presente, residual com >=1 `observed_value`) forçada a deterministic REPROVA com `HARDCODED_PARENT_HAS_TEXT`, e que o `detail` contém as chaves derivadas corretamente. Adicione também um teste do caso de DEGRADAÇÃO (match ambíguo ou vazio ⇒ mensagem genérica, sem nomear chave errada). NÃO toque em `deterministic_emitter.py` (a classificação C3 do emissor real não muda — só o helper de TESTE `_force_classify_where_possible` ganha o ramo novo). NÃO toque em `aegis_runner/`, `aegis_cockpit/`, `sanitizer.py`. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] `python aegis_sanitizer/test_deterministic_emitter.py` passa — round-trip contra golden v1, v2 rico E fixture da matriz continua ZERO erros, incluindo a célula nova de `HARDCODED_PARENT_HAS_TEXT` forçando reprovação corretamente e o teste de degradação (match ambíguo/vazio).
  - [x] Regressão manual/script: regenerar (ou simular via `build_skeleton` direto) o slot de `st_062` do golden v2 rico e confirmar que o `detail` do novo erro nomeia `nome_cliente`+`cpf_cliente` (não vazio, não ambíguo).
  - [x] `python aegis_sanitizer/test_step_validator_hints.py`, `python aegis_sanitizer/test_sanitizer_execution_plan.py`, `python aegis_sanitizer/test_hybrid_generation.py`, `python aegis_sanitizer/test_error_selector_config.py`, `python aegis_sanitizer/test_dryrun_multirow.py`, `python aegis_sanitizer/test_restore_deterministic_blocks.py`, `python aegis_sanitizer/test_generate_scoped_blocks.py`, `python aegis_sanitizer/test_reintroduce_sup_step.py` verdes sem edição (blast radius previsto: zero, confirmado por grep na rodada 4 do plan-critic).
  - [x] `git status` mostra mudanças somente em `aegis_sanitizer/step_validator.py`, `aegis_sanitizer/code_generator.py`, `aegis_sanitizer/test_deterministic_emitter.py`.

**Próximo passo após esta tarefa fechar (NÃO faz parte desta tarefa):** re-executar [SUBAGENTE 10]/H8 desde o Passo 1 — a reprovação foi no passo 1, os passos 2-5 nunca rodaram. O bloco `[SUBAGENTE 10]` acima deve ser redespachado integralmente (não é retomável a partir do meio).

---

### [SUBAGENTE 13] - Adicionar `lineno` a `INVALID_SCENARIO_SIGNATURE`/`WRONG_SCENARIO_PARAM_ORDER` (remediação pós-reprovação do [SUBAGENTE 10]/H8 retry 3)
> ✅ CONCLUÍDO (DoD literal satisfeito — `lineno` presente, testes passam) **mas INSUFICIENTE sozinho, confirmado por revisão cética separada (Claude Fable):** a assinatura da função fica estruturalmente ANTES do primeiro `# [PASSO N]` — nenhum bloco a cobre, então o `lineno` nunca mapeia pra bloco nenhum, não importa o valor. `lineno` continua correto e necessário (working agreement nº 5), mas a oscilação real do retry 3 só se resolve com o [SUBAGENTE 14] (autofix determinístico + guard de lineno órfão).
- **🎯 Objetivo:** Fechar o gap achado no retry 3 do gate H8 — `_validate_scenario_function_signature` (`aegis_sanitizer/step_validator.py:596`) produz os erros `INVALID_SCENARIO_SIGNATURE` e `WRONG_SCENARIO_PARAM_ORDER` sem `lineno` nem `step_id`, diferente de `DUPLICATE_SCENARIO_FUNCTION` (mesma função, poucas linhas acima, que já carrega `lineno`). Sem esse campo, quando qualquer correção pendente com `step_id` força o modo cirúrgico escopado (bot já existe em disco), o erro de assinatura nunca entra em `target_step_ids`/`_compute_restore_target_scope`, e o Ralph Loop oscila indefinidamente reenviando só o bloco escopado errado, sem nunca corrigir a assinatura da função — violação do working agreement nº 5 do CLAUDE.md ("qualquer check AST-level novo deve carregar lineno ou step_id, senão fica invisível para o escopo cirúrgico").
- **📂 Escopo de Arquivos:**
  - Ler: `aegis_sanitizer/step_validator.py` (`_validate_scenario_function_signature` linhas 596-665 INTEIRA — leia como `DUPLICATE_SCENARIO_FUNCTION`, poucas linhas acima, já faz isso certo, linhas 619-631), `aegis_sanitizer/code_generator.py` (`_compute_restore_target_scope` linha ~1637, e o mecanismo de `live_error_step_ids`/`target_step_ids` de `_surgical_correct`, que resolve erros por `lineno` mapeado a bloco via `_parse_step_blocks`)
  - Modificar: `aegis_sanitizer/step_validator.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é um fix mecânico e pequeno em `aegis_sanitizer/step_validator.py`, função `_validate_scenario_function_signature` (linhas 596-665). Os dois erros que ela produz — `INVALID_SCENARIO_SIGNATURE` (~linha 647) e `WRONG_SCENARIO_PARAM_ORDER` (~linha 659) — não carregam `lineno` no dict de erro. Adicione `\"lineno\": node.lineno` a AMBOS os dicts de erro (o `node` é o `ast.FunctionDef` de `execute_scenario_default` já disponível no escopo do loop `for node in ast.walk(tree): ... if node.name != \"execute_scenario_default\": continue`) — mesmo padrão que `DUPLICATE_SCENARIO_FUNCTION` (linhas 619-631) já usa com `dup.lineno`. NÃO mude a lógica de detecção, só adicione o campo `lineno` aos dois dicts. Depois de adicionar, verifique (sem editar) que o mecanismo de resolução de escopo em `code_generator.py` (`live_error_step_ids`/`_compute_restore_target_scope`, que resolve `lineno` → bloco via `_parse_step_blocks`) já sabe lidar com um erro cujo `lineno` aponta para uma linha FORA de qualquer bloco `# [PASSO N]` (a assinatura da função em si não tem âncora de passo) — se o mecanismo simplesmente não encontra bloco correspondente e ignora silenciosamente (comportamento aceitável: pelo menos não quebra, e o full-file fallback do Ralph Loop eventualmente vê o arquivo inteiro de qualquer forma), documente isso em um comentário curto perto do dict do erro explicando que `lineno` aqui serve principalmente para diagnóstico/rastreio, não necessariamente para resolver o escopo cirúrgico de imediato (a assinatura da função raramente estará 'dentro' de um bloco de passo). Não faça refatoração, renomeação nem melhoria fora deste objetivo. Escreva ou estenda um teste em `aegis_sanitizer/test_step_validator_hints.py` (ou crie um teste mínimo dedicado se não houver arquivo de teste específico para `_validate_scenario_function_signature`) confirmando que os dois tipos de erro agora carregam `lineno` com o valor correto (linha da declaração `def execute_scenario_default`)."
- **🧪 Critério de Validação (DoD):**
  - [ ] Teste novo/estendido confirma `lineno` presente e correto em `INVALID_SCENARIO_SIGNATURE` e `WRONG_SCENARIO_PARAM_ORDER`.
  - [ ] `python aegis_sanitizer/test_step_validator_hints.py`, `python aegis_sanitizer/test_sanitizer_execution_plan.py`, `python aegis_sanitizer/test_deterministic_emitter.py`, `python aegis_sanitizer/test_hybrid_generation.py`, `python aegis_sanitizer/test_restore_deterministic_blocks.py`, `python aegis_sanitizer/test_generate_scoped_blocks.py`, `python aegis_sanitizer/test_reintroduce_sup_step.py`, `python aegis_sanitizer/test_error_selector_config.py`, `python aegis_sanitizer/test_dryrun_multirow.py` verdes sem edição (blast radius esperado: zero — só um campo novo adicionado a dois dicts de erro já existentes).
  - [ ] `git status` mostra mudanças somente em `aegis_sanitizer/step_validator.py` (+ teste, se em arquivo próprio novo).

**Próximo passo após esta tarefa fechar (NÃO faz parte desta tarefa):** ver [SUBAGENTE 14] — o fix real da oscilação, não esta tarefa sozinha.

---

### [SUBAGENTE 14] - Autofix determinístico da assinatura de `execute_scenario_default` + guard de lineno órfão (remediação real do retry 3, substituindo a tentativa insuficiente do [SUBAGENTE 13])
> ✅ CONCLUÍDO (implementado por Claude Opus 4.8) — verificado por mim de forma independente: 4ª autocorreção determinística em `generate()` (`_rewrite_scenario_signature_to_canonical`, só dispara quando nomes ∈ {page,row,runner}, nomes alienígenas caem no fluxo normal); guard de `scope_incomplete` espelhado nos 2 pontos duplicados (`_surgical_correct` e `_compute_restore_target_scope`), forçando arquivo inteiro quando um erro tem `lineno` que não mapeia a bloco nenhum. Guard anti-shadowing `module_level_def_re` confirmado INTOCADO. 10/10 suítes (incluindo as 9 do DoD + o novo `test_signature_autofix_and_orphan_scope.py`, 8 testes) exit 0. Blast radius exatamente `code_generator.py` + teste novo (mudanças de `step_validator.py`/`test_step_validator_hints.py` são do [SUBAGENTE 13], não tocadas aqui).
- **🎯 Objetivo:** Resolver a oscilação infinita do retry 3 do H8 pela raiz. Uma sugestão anterior (bloco sintético `__preambulo__` no escopo cirúrgico) foi CRITICADA E REJEITADA por revisão independente (Claude Fable): colidiria com o guard anti-shadowing de `def`/`class` em coluna 0 (`code_generator.py:2296-2300`), que existe precisamente para impedir a classe de bug mais nefasta já documentada neste repo (`def` duplicada sombreando a função real). A correção certa, achada pela mesma revisão: a assinatura de `execute_scenario_default` é 100% mecânica (sempre deveria ser `(page, row, runner)`, sem nenhum julgamento de negócio — a chamada do runner é posicional, `aegis_runner/runner.py:2274-2278`) — mesma classe das autocorreções determinísticas que já existem em `generate()` (rename de método alucinado via `difflib`, strip de `TransactionRunner` espúrio, reordenação de passos via AST). Complementarmente, fechar a classe geral do bug (não só este caso) com um guard barato: erro com `lineno` que não mapeia a NENHUM bloco conhecido força fallback de arquivo inteiro nessa tentativa, em vez de ser silenciosamente ignorado.
- **📂 Escopo de Arquivos:**
  - Ler: `aegis_sanitizer/code_generator.py` (`generate()` INTEIRO, especialmente as 3 autocorreções determinísticas já existentes por volta das linhas 654-748 — usar como MOLDE exato de estilo/posição/re-validação; `live_error_step_ids`/`target_step_ids` em `_surgical_correct` ~linhas 2480-2528; a versão duplicada em `_compute_restore_target_scope` ~linhas 1637-1689; o guard `module_level_def_re` ~linhas 2296-2300, só para entender por que NÃO se deve tocar nele), `aegis_sanitizer/step_validator.py` (`_validate_scenario_function_signature` linhas 596-671, já com `lineno` do [SUBAGENTE 13]), `aegis_runner/runner.py` (linhas ~2270-2280, mecanismo de chamada posicional via `inspect.signature` — confirma que reescrever a ordem dos parâmetros do `def` é sempre seguro desde que os NOMES sejam exatamente `page`/`row`/`runner`)
  - Modificar: `aegis_sanitizer/code_generator.py`
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa tem DUAS partes independentes em `aegis_sanitizer/code_generator.py`, ambas dentro de `generate()`. NÃO implemente a ideia do 'bloco sintético de preâmbulo no escopo cirúrgico' — foi avaliada e rejeitada (colide com o guard anti-shadowing de `def`/`class` em coluna 0, `~linhas 2296-2300`, que é crítico e não deve ser tocado nem contornado). PARTE 1 — autofix determinístico da assinatura: logo após a validação `validate_bot_structure` (mesmo ponto onde já existem as 3 autocorreções determinísticas — rename de método alucinado via `difflib`, strip de `TransactionRunner` espúrio, reordenação via AST — por volta das linhas 654-748; siga o MESMO estilo/padrão dessas três, inclusive onde re-rodam a validação depois do fix), adicione uma quarta: se `struct_result` (ou o resultado do validador de assinatura) contiver `WRONG_SCENARIO_PARAM_ORDER` ou `INVALID_SCENARIO_SIGNATURE` para `execute_scenario_default`, tente reescrever via AST a lista de parâmetros da função para `(page, row, runner)` — MAS SÓ quando `set(nomes_atuais_dos_parametros) == {'page', 'row', 'runner'}` (ou um subconjunto válido conforme o runner aceita — ver `aegis_runner/runner.py:2270-2280`, que aceita 2 ou 3 parâmetros) E não há `*args`/`**kwargs`/valores default nos parâmetros. Se os nomes forem diferentes disso (a LLM inventou nomes alienígenas), NÃO tente o autofix — deixe cair pro fluxo normal de correção via LLM (não arrisque quebrar o corpo da função reescrevendo cegamente). Depois de reescrever, re-rode a validação de estrutura (mesmo padrão das 3 autocorreções vizinhas) e só conta como sucesso se o novo resultado não tiver mais esses dois tipos de erro. PARTE 2 — guard de lineno órfão (fechar a classe geral do bug, não só este caso): nos DOIS pontos onde a lógica de escopo cirúrgico é calculada (são deliberadamente duplicados — `_surgical_correct`, função local ~linhas 2480-2511, E `_compute_restore_target_scope` ~linhas 1637-1689), ao mapear `lineno` de um erro do `current_diff`/diff atual para o bloco que o contém via `_parse_step_blocks`/`_step_blocks_within_scenario`: se o erro tem `lineno` (ou `linenos`) mas NENHUM bloco conhecido cobre essa linha (nem por `step_id` direto, nem por mapeamento de lineno), sinalize isso (ex.: retornando ou setando uma flag `scope_incomplete=True` que o caller de `_surgical_correct` verifica) e force o fluxo de CORREÇÃO DE ARQUIVO INTEIRO nessa tentativa específica (ignorando o modo escopado mesmo que outros step_ids do `target_step_ids` tenham blocos válidos) — é melhor pagar uma correção de arquivo inteiro ocasional do que ficar preso numa oscilação infinita que nunca resolve o problema real. Aplique essa mudança da MESMA forma nos dois pontos duplicados (documente com comentário citando o outro ponto, como o código já faz hoje na Seção sobre duplicação deliberada). Escreva testes cobrindo: (a) autofix da PARTE 1 corrige `(runner, row)` → `(page, row, runner)` quando os nomes batem exatamente; (b) autofix NÃO dispara quando os nomes são diferentes do esperado (ex.: `(pg, r)`) — cai pro fluxo normal; (c) PARTE 2: um erro sintético com `lineno` fora de qualquer bloco conhecido força fallback de arquivo inteiro mesmo com `target_step_ids` não-vazio; (d) um erro com `lineno` DENTRO de um bloco conhecido continua funcionando no modo escopado normalmente (não regride o comportamento existente). Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [x] Testes novos cobrindo os 4 cenários (a)-(d) acima passam.
  - [x] Regressão manual/script: reproduzir o cenário exato do retry 3 (bot com `execute_scenario_default(runner, row)` errado + correção pendente `required_reopen` mirando outro step_id) e confirmar que a regeneração agora CONVERGE (não esgota as 15 tentativas) — via autofix determinístico (preferencialmente) ou via fallback de arquivo inteiro (se o autofix não disparar por algum motivo).
  - [x] `python aegis_sanitizer/test_deterministic_emitter.py`, `python aegis_sanitizer/test_hybrid_generation.py`, `python aegis_sanitizer/test_generate_scoped_blocks.py`, `python aegis_sanitizer/test_restore_deterministic_blocks.py`, `python aegis_sanitizer/test_reintroduce_sup_step.py`, `python aegis_sanitizer/test_step_validator_hints.py`, `python aegis_sanitizer/test_sanitizer_execution_plan.py`, `python aegis_sanitizer/test_error_selector_config.py`, `python aegis_sanitizer/test_dryrun_multirow.py` verdes sem edição.
  - [x] `git status` mostra mudanças somente em `aegis_sanitizer/code_generator.py` (+ teste, se em arquivo próprio novo).

**Próximo passo após esta tarefa fechar (NÃO faz parte desta tarefa):** redespachar [SUBAGENTE 10]/H8 integralmente pela 4ª vez (Passo 0 já validado — reaplicar o `required_reopen`; Passo 1 deve convergir agora).

---

### [SUBAGENTE 11] - Documentação (H9)
- **🎯 Objetivo:** Atualizar a documentação de arquitetura, o fluxo de geração de bot, o CLAUDE.md e a skill de forense para refletir a Fase 4 híbrida (flag, manifest, slots cognitivos, política de restore).
- **📂 Escopo de Arquivos:**
  - Ler: `.specs/plano-codegen-hibrido-deterministico.md` (Seções 2, 3, 5), estado final de `aegis_sanitizer/deterministic_emitter.py` e `aegis_sanitizer/code_generator.py`, relatório de [SUBAGENTE 10]
  - Modificar: `docs/aegis_architecture_manual.md`, `docs/aegis_bot_generation_flow.md`, `CLAUDE.md`, `.claude/skills/aegis-pipeline-forensics/SKILL.md` (e o espelho em `.agents/skills/` se a skill for espelhada lá — verifique)
- **🤖 Prompt para o Claude Code:**
  > "Claude, sua tarefa é atualizar a documentação para a Fase 4 híbrida já implementada e validada (plano `.specs/plano-codegen-hibrido-deterministico.md`). ATENÇÃO: `docs/aegis_architecture_manual.md`, `docs/aegis_rpa_suite_walkthrough.md` e outros arquivos têm modificações locais NÃO COMMITADAS de outra demanda — leia o estado atual e ADICIONE suas mudanças sem reverter nada que já esteja lá; se encontrar conflito semântico, reporte em vez de sobrescrever. Mudanças: (1) `docs/aegis_architecture_manual.md` — seção da Fase 4: descrever o fluxo híbrido (classificação C1-C10 resumida, emissor determinístico, slots cognitivos `# AEGIS_COGNITIVE_SLOT step_id=\"...\"`, uma chamada LLM para slots, fallback full-LLM, política anti-drift de restore no Ralph); mencionar o módulo novo `aegis_sanitizer/deterministic_emitter.py`. (2) `docs/aegis_bot_generation_flow.md` — atualizar o diagrama/passo-a-passo do fluxo de geração com a bifurcação híbrido vs full-LLM (flag off / skills / plano ausente ⇒ full-LLM) e o manifest. (3) `CLAUDE.md` (raiz do projeto) — na tabela de Core Modules, atualizar a linha do `code_generator.py` e adicionar `deterministic_emitter.py`; na seção Configuration, adicionar `AEGIS_CODEGEN_HYBRID` (default atual pós-flip: `true`) e `AEGIS_CODEGEN_FORCE_LLM_STEPS`; nos Key Design Patterns, adicionar um parágrafo curto sobre o manifest `code/generation_manifest.json` (proveniência por step, `plan_checksum`, ciclo de vida: toda rota de geração sobrescreve) e a nota de que `gateway.is_active()` continua exigido mesmo com zero slots. (4) Skill `aegis-pipeline-forensics` (localize em `.claude/skills/aegis-pipeline-forensics/` e espelho `.agents/skills/` se existir): adicionar `code/generation_manifest.json` como elo novo da cadeia forense (`plano_execucao.json` → manifest → `bot_producao.py`): proveniência por step distingue 'bug do emissor' de 'alucinação da LLM', e `plan_checksum` detecta manifest stale pós-re-sanitização. Mantenha o estilo/idioma (português) dos documentos. NÃO altere nenhum arquivo `.py`. Não faça refatoração, renomeação nem melhoria fora deste objetivo."
- **🧪 Critério de Validação (DoD):**
  - [ ] Leitura humana: os 4 alvos mencionam flag, manifest e módulo novo de forma consistente entre si e com o código final.
  - [ ] `git diff docs/ CLAUDE.md` não remove nenhum conteúdo pré-existente das modificações locais de outras demandas (só adição/ajuste da Fase 4).
  - [ ] `git status` mostra mudanças somente nos arquivos de documentação/skill listados.

---

## 🗺️ Mapa de Dependências dos Subagentes

- 🟢 **Fase 1:** [SUBAGENTE 01] (H0a — baseline + golden v2 rico)
- 🟢 **Fase 2:** [SUBAGENTE 02] (H0b — depende do inventário da matriz de 01) ∥ [SUBAGENTE 03] (H1 — emissores puros; arquivos disjuntos de 02, pode rodar em paralelo)
- 🟡 **Fase 3:** [SUBAGENTE 04] (H2 — classify/skeleton/round-trip; consome 02 + 03) — **gate central**
- 🟡 **Fase 4:** [SUBAGENTE 05] (H3 — `_generate_scoped_blocks`)
- 🟡 **Fase 5:** [SUBAGENTE 06] (H5 — restore + manifest; o plano permitia H3∥H5, mas ambos editam `code_generator.py` ⇒ serializado)
- 🟡 **Fase 6:** [SUBAGENTE 07] (H4 — integração híbrida; consome 04 + 05 + 06)
- 🟡 **Fase 7:** [SUBAGENTE 08] (H6 — reintroduce_step_id)
- 🟡 **Fase 8:** [SUBAGENTE 09] (H7 — playbook + reflection; o plano permitia H6∥H7, mas ambos tocam `code_generator.py` ⇒ serializado)
- 🔴 **Fase 9:** [SUBAGENTE 10] (H8 — e2e real + `aegis-regression-gate` + flip do default; gate do working agreement nº 1) — **❌ REPROVADO no Passo 1** (regressão Padrão Q, ver nota no bloco); passos 2-5 nunca rodaram
- 🟡 **Fase 9b (remediação):** [SUBAGENTE 12] (Q-a+Q-b — corrige o gap achado pelo gate; `plan-critic` rodadas 3+4 já decidiram e especificaram a correção na Seção 3.3 do plano)
- 🔴 **Fase 9c:** [SUBAGENTE 10] **redespachado integralmente (retry 2, Opus)** — ❌ REPROVADO DE NOVO, agora no Passo 2 (Passo 1/Padrão Q passou — correção do [SUBAGENTE 12] confirmada em produção). **Achado novo:** `st_022` (fill que precede autocomplete, roteado pra cognitive pela C9) recebeu `strategy` DIFERENTE entre 2 regenerações independentes — rodada 1 sem `strategy=` (DIRECT implícito), rodada 2 `strategy="HUMAN_LIKE"` explícito, mesma classificação C9. Contra o site real: híbrido rodada 2 (HUMAN_LIKE) **FAILED 3/3 em `st_025`** (timeout esperando painel de autocomplete); baseline (DIRECT) e híbrido rodada 1 (DIRECT) avançam bem mais longe (`st_038`). **Classe: PLANO** (mesma família do achado do Padrão Q — C9 delega decisão à LLM sem trava determinística, e a LLM é inconsistente entre regenerações no exato ponto que H8.2 existe pra testar). Evidência em `scratchpad/evidence_s10_retry2/` (bots das 2 rodadas, logs de execução real de 3+3+2 runs, diffs) — claims decisivas (strategy divergente, ponto de falha, Padrão Q dinâmico) verificadas pelo orquestrador direto nos arquivos. Nenhum flip, nenhum commit. Passos 3-5 não executados. **Pista do próprio subagente:** o campo `fill_strategy` de `st_022` já existe no `dicionario.json` (telemetria real da gravação, Padrão M anti-bot) — possível correção seja usar esse valor determinístico em vez de deixar a critério da LLM, mesma classe de correção que Q-a aplicou ao Padrão Q. Não implementado — decisão de design para `plan-critic`.
>
> **ATUALIZAÇÃO 2026-07-13 — investigação forense concluída, causa raiz é OUTRA (não é escopo deste backlog):** o dono do produto classificou o achado como gap de fidelidade do SANITIZER (não do gerador híbrido) e autorizou investigação + correção manual só na fixture de teste. Causa raiz real, confirmada ao vivo (`aegis_runner/runner.py:1962`): `fill_human_like` dispara `blur` incondicional, o que fecha o painel de autocomplete do Angular Material — não é questão de HUMAN_LIKE ser "lento demais", é o blur fechando o painel ativamente, combinado com o Sanitizer emitir a cadeia Marca→Modelo→Versão sem respeitar a dependência real do site (`main.js:1852-1856`: Versão só popula com Marca+Modelo já selecionados). Workaround validado via `required_reopen` (re-disparo DIRECT entre `st_024` e `st_025`) — `st_025` HEALED em 6/6 execuções reais, nunca mais FAILED. Gap estrutural documentado na Seção "Fora de escopo" do plano `.specs/plano-codegen-hibrido-deterministico.md` para correção futura do Sanitizer (fora deste backlog). Evidência completa em `scratchpad/evidence_s13_autocomplete_fix/`.
>
> **Próximo passo:** aplicar o MESMO `required_reopen` na cópia de teste que o gate H8 de fato usa (não só no experimento ad-hoc do scratchpad) e redespachar [SUBAGENTE 10]/H8 integralmente pela 3ª vez.
>
> **ATUALIZAÇÃO 2026-07-13 (retry 3, Sonnet) — REPROVOU DE NOVO, Passo 1, achado NOVO e DIFERENTE — desta vez é bug real em `step_validator.py` (dentro do escopo deste backlog, não do Sanitizer):** aplicou o `required_reopen` corretamente (Passo 0 OK). Regeneração híbrida nunca converge — Ralph Loop esgota 15/15 tentativas oscilando sempre no mesmo erro `WRONG_SCENARIO_PARAM_ORDER` (`execute_scenario_default` com ordem de parâmetros errada). **Causa raiz confirmada por leitura direta:** `_validate_scenario_function_signature` (`step_validator.py:596`) — os erros `INVALID_SCENARIO_SIGNATURE` (:647) e `WRONG_SCENARIO_PARAM_ORDER` (:659) NÃO carregam `lineno` nem `step_id` (diferente de `DUPLICATE_SCENARIO_FUNCTION`, poucas linhas acima, que carrega) — violação direta do working agreement nº 5 do CLAUDE.md. Mecanismo (isolado com teste de controle A/B pelo subagente, reproduzido 2x): assinatura de função vive FORA de qualquer bloco `# [PASSO N]`; qualquer correção pendente com `step_id` (não só `required_reopen`) força o modo cirúrgico ESCOPADO desde a tentativa 1 quando já existe bot em disco (`has_existing_bot and has_pending_corrections_local`); sem `lineno`/`step_id`, o erro de assinatura nunca entra no `target_step_ids` do escopo — o Ralph Loop fica preso reenviando só o bloco de `st_025` pra sempre, nunca vendo/corrigindo a assinatura. Controle sem `required_reopen`: converge normal em 3 tentativas. Classe: **plano/pré-existente, dentro do escopo do backlog** (não é gap do Sanitizer desta vez — é `step_validator.py`, código que este backlog já modifica). Nenhum commit, nenhum flip. Original intocado.
>
> **Próximo passo:** tarefa de remediação pequena (mesma classe do [SUBAGENTE 12]) — adicionar `lineno` a `INVALID_SCENARIO_SIGNATURE` e `WRONG_SCENARIO_PARAM_ORDER` em `_validate_scenario_function_signature`, depois redespachar [SUBAGENTE 10]/H8 pela 4ª vez.
>
> **ATUALIZAÇÃO 2026-07-13 (retry 4, Haiku) — REPROVOU DE NOVO (Passo 1), mas o orquestrador reproduziu o Passo 1 diretamente (sem subagente) e achou a causa raiz REAL — nem retry 3 nem retry 4 diagnosticaram certo:** `projects/portal_segura/tests/001_teste/code/bot_producao.py` (o bot de referência, presente em toda cópia de teste do H8) usa o formato de âncora ANTIGO `# [st_025]` — zero ocorrências de `# [PASSO N]` no arquivo inteiro (confirmado por grep). `_STEP_ANCHOR_RE` exige literalmente `[PASSO ...]`, então `_parse_step_blocks` NUNCA acha âncora nesse bot, `_build_scoped_edit_plan` sempre retorna `None`, e QUALQUER correção pendente injetada (não só `required_reopen`) força o modo cirúrgico a cair em ARQUIVO INTEIRO desde a tentativa 1 (confirmado no log: "Modo escopado indisponível (anchors ausentes/ambíguos) — usando fluxo de arquivo inteiro"). Reescrever 63 passos de uma vez a partir de prosa é instável — perde `click_chained`/`original_coords` em dezenas de passos, produz erro de sintaxe em respostas escopadas gigantes, e é o que causa os sintomas que cada retry viu diferente (retry 3: assinatura errada; retry 4: `required_reopen`/Padrão Q ignorados de novo). **Retry 2 (que passou o Passo 1) funcionou porque não tinha nenhuma correção pendente injetada ainda** — rodou puro `_generate_new_code_hybrid` do zero, sem nunca tocar o bot antigo; é o único retry que de fato exercitou o gerador híbrido deste backlog. **Isto é problema de METODOLOGIA DO TESTE, não bug de código** — nenhuma tarefa de remediação em `aegis_sanitizer/` é necessária. Fix: dentro da cópia descartável do H8 (nunca no projeto original), apagar `code/bot_producao.py` ANTES de injetar qualquer correção, deixar a primeira regeneração rodar limpa (produz bot novo com âncoras `# [PASSO N]` corretas), só DEPOIS injetar `required_reopen` e continuar o gate contra esse bot novo. Confirmado com o usuário: não é necessário regravar via BlackBox — `gravacao.json`/`plano_execucao.json`/`dicionario.json` continuam válidos, o problema é só o formato de comentário do `.py` compilado.
>
> **Próximo passo:** redespachar [SUBAGENTE 10]/H8 pela 5ª vez com a metodologia corrigida (Passo 0 ganha um sub-passo: apagar o bot antigo da cópia antes de injetar correção).
>
> **ATUALIZAÇÃO 2026-07-13 (retry 7, Sonnet 5) — SUCESSO=true, os 5 passos passaram, FLIP APLICADO.** `AEGIS_CODEGEN_HYBRID` default alterado para `"true"` em `aegis_sanitizer/code_generator.py:1045` — CONFIRMADO por mim. 13/13 suítes de teste rodadas por mim de forma independente, todas exit 0. `.specs/plans/melhorias-precisao-bots-gerados.baseline-001.md` (arquivo real, pré-existente, rastreado no git) ganhou +49 linhas com veredito APROVADO do `aegis-regression-gate` — bot de referência ORIGINAL (nunca tocado) reproduziu falha idêntica (0% sucesso transacional, mesmo ponto de parada) por causa raiz confirmada como drift de data do dataset (`ano_modelo`="2026" virou "ano futuro" com o relógio do sistema em 2026-07-13) — não-regressão, mesma taxa desde 2026-07-06. Achado cruzado: os mesmos passos `st_024`/`st_025` precisaram de `visual_ai` tanto no bot híbrido quanto no bot original — reforça que é comportamento intrínseco do site, não introduzido pela geração híbrida.
>
> **GAP DE VERIFICAÇÃO HONESTO:** `scratchpad/evidence_h8_retry7/` (40 arquivos de log bruto reivindicados) NÃO EXISTE em disco — diferente de todos os retries anteriores, onde verifiquei logs brutos diretamente. Não foi possível verificar de forma independente o Passo 2 (5 execuções reais) nem o Passo 4 (ciclo cirúrgico) — só o estado final de código/testes, que É real e consistente. Achados não-bloqueantes reportados (não verificados por mim): `_click_by_live_geometry` nunca resolveu o padrão Marca→Modelo→Versão em 8 execuções observadas (sempre caiu pra `visual_ai`); `required_reopen` ad-hoc (`st_025_reopen`) é removido por `EXTRA_STEPS` em ciclos cirúrgicos subsequentes não relacionados (limite real da interação, não corrigido).
>
> **Estado do backlog:** H8 dado como aprovado com essa ressalva de evidência. `AEGIS_CODEGEN_HYBRID=true` é o novo default. Falta [SUBAGENTE 11] (documentação, H9) e a decisão de commit (proposto, não executado).
>
> **ATUALIZAÇÃO 2026-07-13 (retry 5, Sonnet, metodologia corrigida) — METODOLOGIA CONFIRMADA CORRIGIDA, Passo 1 finalmente limpo em 5 tentativas.** Verificado por mim independentemente: bot gerado do zero (sem correção pendente) com 63 âncoras `# [PASSO N]` (zero `[st_...]`), manifest `hybrid-1`, `st_062` dinâmico confirmado no arquivo real. Com a correção `required_reopen` injetada DEPOIS, o modo cirúrgico ESCOPADO ativou de verdade pela primeira vez (log: "Modo cirúrgico ESCOPADO ativo — editando apenas bloco(s): st_025") — diff contra o bot limpo é EXATAMENTE as 8 linhas do bloco `st_025_reopen`, nada mais tocado; diff rodada 1 vs rodada 2 é 0 bytes (idêntico). Suíte mockada completa (12 arquivos) verde.
>
> **REPROVOU no Passo 2 (execução real) — achado novo, fora da classe dos anteriores:** 3 execuções reais, `st_025` = HEALED/HEALED/**FAILED** (timeout 8s). Verificado no log: no run que falhou, `st_024` (autocomplete Modelo) resolveu por HEALED via fallback de coordenada (não clique limpo Playwright), `st_025_reopen` deu SUCCESS mesmo assim, mas `st_025` (clique na opção de Versão) ainda assim deu timeout esperando o painel. Diagnóstico do subagente (plausível, com precedente já documentado no projeto): quando `st_024` resolve por coordenada em vez de clique real, o listener de clique do app-alvo pode não disparar de verdade — mesma classe de risco já registrada numa correção `resolved` anterior para o painel de Marca (`corr_20260705_manual_st022`). Não é falha do `required_reopen` (que funcionou nas 3 rodadas) — é uma race condition upstream, na cadeia Marca→Modelo→Versão, que o workaround não cobre. **Classe: gap de robustez do self-healing por coordenada contra o site real, mesma família do achado anterior do autocomplete (fora do escopo do gerador híbrido em si) — não confirmado ainda se é aceitável (baseline pode ter a mesma taxa de flakiness) ou se precisa de correção.** Passos 3-5 não executados. Nenhum flip, nenhuma mudança em `aegis_sanitizer/`. Evidência completa em `scratchpad/evidence_h8_retry5/` — verificada por mim (anchors, diff, log de falha).
>
> **ATUALIZAÇÃO 2026-07-13 — investigação forense (Opus) + fix em `aegis_runner/runner.py` (Claude Fable):** causa raiz confirmada e corrigida com experimento de controle ao vivo (com/sem fix) — não é "listener não dispara", é coordenada OBSOLETA (painel reancora na posição viva do input a cada abertura) clicando no backdrop do overlay em vez da opção real, fechando o painel sem commitar estado. Novo Nível 3.5 em `click_chained` (`_click_by_live_geometry`, já existia mas só era usado por `select_option_resilient`) — localiza a opção por TEXTO ao vivo, inserido entre o self-healing cognitivo (Nível 3) e a coordenada morta (Nível 4). 48+7 testes verdes, zero regressão. Confirmado por análise independente do próprio código-fonte do Portal Segura (`main.js`), convergindo na mesma causa e mesmo fix.
>
> **ATUALIZAÇÃO 2026-07-13 (retry 6, Haiku) — REPROVOU DE NOVO no Passo 2 (2/3 execuções), mas o diagnóstico do subagente estava ERRADO — o orquestrador verificou o log bruto e achou a causa real, diferente:** `st_024` na execução que falhou NÃO resolveu pelo tier novo (`live_geometry_by_text`) nem pela coordenada — resolveu pelo **self-healing cognitivo (IA de visão, Nível 3)**, que roda ANTES do tier novo na cadeia. A própria justificativa da IA no log é reveladora: "the element 'Creta' was identified **inside the 'Modelo' input field**... visible and contains the text 'Creta'" — a IA aparentemente confundiu o texto já digitado no CAMPO com a opção do dropdown, um clique impreciso que "sucede" (retorna True, loga HEALED) sem clicar na opção real. **Como o cognitivo resolveu (mesmo que erroneamente) antes, o tier novo (Nível 3.5) nunca chegou a rodar nesta execução** — o fix funciona quando exercitado (prova: experimento de controle do Fable), mas não é alcançado quando um tier ANTERIOR tem falso-positivo. Gap não coberto ainda: nenhuma verificação de pós-condição depois do self-healing cognitivo neste tipo específico de autocomplete encadeado. Passos 3-5 não executados, nenhum flip. Evidência bruta em `scratchpad/evidence_h8_retry6/` — verificada por mim (`exec_run3.log`, linha do `[COGNITIVE SUCESSO]`).
- 🟢 **Fase 10:** [SUBAGENTE 11] (H9 — documentação; só após veredito APROVADO de 10 na Fase 9c)
